> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec 08](../../architecture/features/08-verification-trust.md) · [feature spec 17](../../architecture/features/17-terminal-tui-client.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 4.28 — Docs sync + phase acceptance-demo wiring

**Phase:** 4 — Verification & Trust + Terminal TUI Client · **Feature:** T08 + T17 (phase exit gate) · **Depends on:** all above · **Status:** pending

## Goal
Close out the phase: docs in sync, both acceptance demos runnable end to end, exactly as the phase
README's exit criteria describe.

## Scope
- **In:** [tui-client.md](../../architecture/tui-client.md) kept in sync with what actually shipped;
  [tui-screen-flow.mermaid](../../architecture/diagrams/tui-screen-flow.mermaid) finalized against the
  real screen stack; both ADRs (0020, 0021) cross-linked from the code that implements them; T08's
  `meridian-mitm-sim` demo script and T17's onboarding→verified-chat→restart→key-change-blocks demo
  script (from the feature specs' "Working output" sections) both run end to end, exactly as written.
- **Out:** any new feature code — this task is verification + doc reconciliation only.

## Deliverables
1. Doc-sync pass (`tools/check-docs.sh` clean; `tui-client.md` and the screen-flow diagram match
   shipped behavior).
2. Both feature specs' demo scripts run as documented — captured as the phase's acceptance evidence.
3. `docs/tasks/phase-4/README.md` exit criteria all checked.

## Tests
`bash tools/check-docs.sh` · T08's demo script (`meridian-mitm-sim --attack substitute-key --against verified/tofu`) · T17's demo script (onboarding → verified chat → restart → key-change block), both from the feature specs' "Working output" sections.

## Reviews
- **reviewer** (combined) — final consistency pass across the whole phase's diff.

## Links
- Feature specs: [08-verification-trust.md](../../architecture/features/08-verification-trust.md),
  [17-terminal-tui-client.md](../../architecture/features/17-terminal-tui-client.md)
- PR: <filled at /next-task time>

## Status
Pending.
