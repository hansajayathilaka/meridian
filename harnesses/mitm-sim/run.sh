#!/usr/bin/env bash
# mitm-sim harness (scaffold stub). Wired into CI so it exists from day one.
# Real implementation lands with its feature. Until then this is a no-op that PASSES,
# and MUST be replaced (not deleted) when the guarded feature is implemented.
set -euo pipefail
echo "[mitm-sim] scaffold stub — TODO: implement per docs/testing/strategy.md before the guarded feature merges."
exit 0
