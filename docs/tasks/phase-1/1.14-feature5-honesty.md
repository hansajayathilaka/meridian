<!-- Fix-task for Phase 1 review finding F11 (honesty portion). -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report finding F11](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.14 — Feature 5 honesty: bound coturn credential reuse + truthful claim

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F11 honesty) · **Depends on:** — · **Status:** done

## Goal
TURN credential reuse is actually bounded, and the "single-session, reuse rejected" claim matches real
behavior — closing the blocking overclaim without waiting on the webrtc backend / packet capture.

## Scope
- **In:**
  - Enable `user-quota` (and reconsider `use-auth-secret`) at `infra/coturn/turnserver.conf:50` — today
    both are commented out, so a captured credential admits unlimited allocations until expiry.
  - Reword the "single-session, reuse rejected" claim at `apps/transport/src/types.rs:93` and the test at
    `apps/rendezvous/tests/rendezvous.rs:165` to describe actual behavior (grants are distinct; reuse is
    bounded by quota, not rejected outright) until true single-session rejection is enforced.
- **Out:** the netns/tcpdump packet-capture acceptance proof (→ 1.23, split from what was originally
  1.16); the webrtc backend (→ 1.15).

## Deliverables
1. Edited `infra/coturn/turnserver.conf` — `user-quota=4` enabled, with rationale documented inline
   (1 control-plane allocation + a small number of concurrent P2P legs).
2. Corrected wording/comments across the actual claim sites (verified by repo-wide grep — the line
   cited above, `apps/transport/src/types.rs:93`, turned out to be stale/no such claim exists there;
   the real "single-session, reuse rejected" language was in `apps/rendezvous/src/turn.rs`,
   `config.rs`, `ws.rs`, `apps/proto/src/msg.rs`, `frame.rs`, `apps/signaling/src/client.rs`, plus
   `docs/api/rendezvous-protocol-v1.md`, `docs/architecture/features/05-nat-traversal-relay-policy.md`,
   and the `nat-matrix` harness docs/scripts — all reworded to the honest version).
3. Renamed/adjusted the rendezvous test `each_turn_grant_is_single_session` →
   `each_turn_request_mints_a_distinct_credential` (assertions unchanged) matching the true guarantee.

## Risks / notes
Do not claim reuse is "rejected" if quota merely bounds it — the fix is to make the claim honest. Land
**before** 1.23 (which proves the policy at the wire level). Confirm the coturn config still starts
(`deployment` skill) after enabling the quota.

## Tests
`cargo nextest run -p meridian-rendezvous` · coturn config smoke check (`turnserver -c … --check` if
available, or the deployment skill's validation step).

## Reviews
**security-reviewer** (relay credential model; no overclaim) — APPROVE, no blocking findings
(non-blocking nit: `user-quota=4` has no `TODO: confirm` marker even though real concurrency needs
are unvalidated pending 1.15/1.23 — worth a follow-up, not a defect now).
**devops** (coturn config valid) — APPROVE; structurally well-formed, no conflict with `total-quota`/
`bps-capacity` (still deferred to T14), takes effect via the existing compose bind-mount with no
further infra change needed.

## Links
- Report: [F11](./review-report.md) (honesty portion); wire proof → 1.23 (split from what was originally 1.16)
- Skill: [deployment](../../../.claude/skills/deployment/SKILL.md), [webrtc-nat-traversal](../../../.claude/skills/webrtc-nat-traversal/SKILL.md)
- PR: <filled at /next-task time>
