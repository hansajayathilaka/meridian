{
  "name": "shared-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic MeridianClientAdapter TS boundary + shared Svelte visual primitives for apps/web and clients/desktop (ADR 0012, task 12.2). Not yet consumed by a real app — apps/web is still a scaffold (see apps/web/CLAUDE.md); wired in by 12.6/12.7-12.9/12.13.",
  "main": "./src/lib/index.ts",
  "svelte": "./src/lib/index.ts",
  "types": "./src/lib/index.ts",
  "exports": {
    ".": {
      "svelte": "./src/lib/index.ts",
      "types": "./src/lib/index.ts",
      "default": "./src/lib/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.json",
    "lint": "echo 'TODO: eslint'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "@testing-library/svelte": "^5.4.2",
    "@tsconfig/svelte": "^5.0.4",
    "jsdom": "^25.0.1",
    "svelte": "^4.2.19",
    "svelte-check": "^3.8.6",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.4"
  },
  "peerDependencies": {
    "svelte": "^4.0.0"
  }
}
