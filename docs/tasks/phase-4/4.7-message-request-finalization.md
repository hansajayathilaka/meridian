> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/08-verification-trust.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 4.7 — Message-request UX finalization (from T06)

**Phase:** 4 — Verification & Trust + Terminal TUI Client · **Feature:** T08 — Verification & Contact Trust · **Depends on:** 4.3, 4.6 · **Status:** pending

## Goal
Close the loop between T06/task 2.10's first-contact gate (already implemented in
`apps/core/src/chat.rs`: `MessageRequest`, `pending_requests`, `accept_request`, `reject_request`) and
T08's trust module: accepting a request TOFU-pins a real `Contact`.

## Scope
- **In:** glue code in `trust.rs`/CLI wiring the existing `chat.rs` request primitives to `trust.rs`:
  `accept_request` → TOFU-pin a `Contact` + offer petname assignment inline; `reject_request` leaves no
  trace (no contact created, no state retained).
- **Out:** any change to `chat.rs`'s existing `MessageRequest` machinery — this task is glue only, not a
  rework of 2.10's gate. The TUI's Requests screen (4.21).

## Deliverables
1. Glue module/function wiring `chat.rs`'s accept/reject into `trust.rs`'s TOFU-pin + petname flow.
2. Tests: accept → pinned contact with petname prompt; reject → no `Contact` record exists.

## Risks / notes
- Keep this a thin adapter — the request gate's security properties (sender key + safety number + intro
  shown before acceptance) are already correct from 2.10; don't re-derive them here.

## Tests
`cargo nextest run -p meridian-cli --test request_finalization`

## Reviews
- **reviewer** (combined) — thin glue over already-reviewed primitives (2.10's gate, 4.3's trust store).

## Links
- Feature spec: [08-verification-trust.md](../../architecture/features/08-verification-trust.md)
- Prior task: [2.10 — first-contact message-request gate](../phase-2/2.10-message-request-gate.md)
- PR: <filled at /next-task time>

## Status
Pending.
