> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/17-terminal-tui-client.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 4.49 — Persist the accepted sender's intro into `history.jsonl` (fix for 4.48's fifth defect)

**Phase:** 4 — Verification & Trust + Terminal TUI Client · **Feature:** T17 — Terminal TUI Client (gap closure, fix for 4.48's fifth defect) · **Depends on:** 4.42, 4.44 · **Status:** pending

## Goal
Make an accepted message request's intro message survive into the responder's own transcript: today
`worker::run_accept_request` writes `sessions.bin`/`trust.bin`/`contacts.json` on accept but never
`history.jsonl`, so the very first message of every accepted conversation is silently, permanently
absent — live and after restart. This closes the fifth defect the phase's exit gate found (task 4.48),
the remaining blocker on T17's own acceptance criterion "restart restores contacts, **history**, and
ratchet state … local dedup by message id."

## Pre-code consult assessment — recorded here, not deferred
**No mandatory pre-code architect/security-reviewer consult is required.** Compared against why 4.40 and
4.42 needed one: 4.40 extended decrypted key material's in-memory residency for the whole session
lifetime — a real security-posture question with no existing precedent. 4.42 faced a structurally forced
choice between severable shapes, decided by an architect consult because `TrustStore::observe`'s
`hint == ""` output made one candidate workaround provably impossible — a genuine design decision with
real alternatives. This task has neither.

4.48's own Status section posed the question as a live choice between two shapes: (1) write inside
`run_accept_request` itself, or (2) a follow-up `Effect::PersistHistory` dispatched by `App` after
`AcceptRequest` completes, mirroring how `App::apply_accepted_request` reconciles other side effects.
Reading the actual code resolves this, not just favors one option:

- **Every existing `App`-side reconciliation method for a completed effect is purely in-memory.**
  `apply_accepted_request`, `apply_added_contact`, `apply_loaded_history`, and `apply_rejected_request`
  (`apps/tui/src/app.rs:2212-2422`) only ever replay state the worker *already made durable* — none of
  them triggers a **new** write-effect as a side effect of reconciling a completion. Shape 2 would
  introduce a "completion triggers a fresh write" mechanism with no precedent anywhere in this crate,
  and it would open a real durability gap: the accept becomes durable in `trust.bin`/`contacts.json` at
  the end of `run_accept_request`'s single async call, then the intro write would only happen later, as
  a *second*, separately-dispatched effect — a crash in between leaves the peer accepted and reachable
  but with no way to ever recover the discarded `MessageRequest.intro` (it is removed from
  `pending_requests` the moment `accept_request` runs and lives nowhere else).
- **Shape 1 needs zero new mechanism.** `run_accept_request` already writes three sealed documents
  (`sessions.bin`, `trust.bin`, `contacts.json`) inside one function, under the one `(store, handle)`
  pair it already holds (`worker.rs:1245-1288`) — exactly the same "one function performs every write
  this decision requires" pattern `run_add_contact` also uses. Adding a fourth call to
  `crate::store::history::append` — the same writer `worker::run_persist_history` already calls
  identically (`hex::encode(request.peer_pubkey)` + `(store, handle)`, `worker.rs:1773-1782`) — extends
  that pattern by one line's worth of new call, not a new architecture.
- **The "where does `mid` come from" question 4.48 flagged as the crux is already answered by the
  existing type**, not an open design choice: `MessageRequest.intro: ChatContent` is either
  `ChatContent::Text { id, body }` or `ChatContent::Receipt { ack }` (`apps/envelope/src/chat.rs:18-30`
  — only two variants exist today). The ordinary inbound-Text handler
  (`worker::process_inbound_delivery`, `worker.rs:2273-2306`) already mints its `HistoryEntry.mid` from
  exactly this same `id` field (`mid: hex::encode(id)`) — the intro carries the identical wire-level id
  the sender minted, with no new id-generation or effect-field-widening needed.

This is much closer to 4.46's situation ("the fix shape is an almost line-for-line mirror of an existing,
already-reviewed precedent … There is no ADR-adjacent identity or residency question here") than to
4.40's or 4.42's. Ordinary post-diff `reviewer`/`test-engineer` sign-off (below) is sufficient. **If
implementation reveals a genuine design choice this planning pass did not anticipate, stop and flag it
explicitly rather than deciding silently** — but this planning pass, reading `run_accept_request`,
`MessageRequest`, `ChatContent`, and `history::append` directly, found none.

## Scope
- **In:**
  1. `worker::run_accept_request` (`apps/tui/src/worker.rs:1245-1288`): capture the actual
     `Option<MessageRequest>` `chat.accept_request(&request.sender_ik)` returns (today collapsed to
     `.is_some()` at line 1254 and discarded) instead of only its presence. On the genuine, fresh-accept
     branch (`Some(request_taken)` — **not** the `pin_still_owed`-only retry branch, which has no
     `MessageRequest` to source content from: `chat.accept_request` already returns `None` there, since
     the entry was removed from `pending_requests` by the first, partially-successful attempt), and
     **after** the existing `trust.bin`/`contacts.json` writes succeed: if `request_taken.intro` is
     `ChatContent::Text { id, body }`, build a `HistoryEntry` with the exact same shape the ordinary
     inbound-Text handler already builds (`v: crate::store::history::CURRENT_VERSION, mid:
     hex::encode(id), dir: Direction::In, ts: now, stream: "mrd.chat/1", body: body.clone(), state:
     MessageState::Received` — reusing the `now` already computed earlier in the function for
     `trust.observe`, since `MessageRequest` carries no original-arrival timestamp to source a more
     precise one from) and call `crate::store::history::append(&hex::encode(request.sender_ik), &entry,
     store, handle)` — the same writer, the same call shape, `worker::run_persist_history` already uses.
     If `request_taken.intro` is `ChatContent::Receipt { .. }` (the only other variant `ChatContent` has
     today — reachable only from a degenerate/adversarial first envelope, since a receipt as a *first*
     message makes no protocol sense, but the gate in `open_inbound_gated` fires before content-type is
     inspected, so it is type-reachable), **skip the history write** — do not invent a receipt-shaped
     history entry, do not error. This mirrors the existing display-layer precedent
     (`screens/requests.rs::intro_summary`, `"(not a text message — a delivery receipt)"`) rather than
     inventing new handling for a case the crate already has an answer for.
  2. Update `run_accept_request`'s own doc comment to describe this fourth write, in the same style it
     already documents the trust/contacts writes, and to name the narrowed partial-failure window this
     introduces (see Risks below) — extending, not replacing, its existing partial-failure discussion.
  3. Resolve `AcceptRequestRequest`'s own doc comment (`apps/tui/src/app.rs:684-703`), which explicitly
     deferred "the intro → transcript question" to a future task, citing exactly this shape
     (`Effect::PersistHistory` → `crate::store::history`) as the eventual mechanism. Replace the
     "deferred on purpose" paragraph with one recording this task's resolution, mirroring how 4.42 itself
     replaced an earlier version of that same paragraph.
  4. Remove (or convert to a plain assertion that production already did it — implementer's call,
     documented) the hand-rolled `persist_entry(...)` workaround in
     `apps/tui/tests/live_session_e2e.rs` (`AcceptRequest` call at ~617-651, helper at ~714-744) — now
     redundant, since production performs the equivalent write.
- **Out:** the `^N`/`^V` doc-only keybinding nit 4.48 also found (unrelated root cause; stays in
  [phase-4/README.md](./README.md#tasks-todo)'s "Findings with no task yet," not folded in here — mirrors
  the 4.46/4.47 split); the already-tracked `run_accept_request` partial-failure repair-action follow-up
  for a missing `contacts.json` row (this task names a narrower, history-only analog — see Risks — but
  does not fix either; fold the history-only analog into that same tracked follow-up rather than opening
  a third, freestanding partial-failure task); Shape B (`OpenChat`-style transition, already severed by
  4.42, unrelated); any change to `ts` semantics beyond using accept-time as the best available
  approximation (no design doc tracks original-request-arrival-time, and none is invented here); any new
  wire type, protocol, or crypto change; widening `ChatContent`/`MessageRequest`'s own schema; the
  interleaving-gap defect class 4.42/4.46 each fixed generically (`apply_accepted_request`'s stack walk)
  — this task's disk write is independent of which screen is on top, and 4.44's already-built
  `apply_loaded_history` dedup-by-`mid` handles in-memory reconciliation regardless of interleaving, so
  nothing further is needed here.

## Deliverables
1. `worker::run_accept_request` captures and uses the real `Option<MessageRequest>`, persisting the
   intro via `crate::store::history::append` on a genuine accept, exactly as scoped above.
2. `run_accept_request`'s doc comment extended (not replaced) to describe the fourth write and its
   partial-failure window.
3. `AcceptRequestRequest`'s doc comment (`app.rs:684-703`) updated to record this task's resolution of
   the previously-deferred "intro → transcript" question, citing this task.
4. `apps/tui/tests/live_session_e2e.rs`'s manual `persist_entry(...)` workaround removed or converted to
   an assertion (Scope point 4), documented either way.
5. Extended coverage in `apps/tui/tests/run_worker_trust.rs` (natural site: extend
   `accept_request_delivers_the_session_and_tofu_pins_the_sender_with_an_empty_hint`, or add a dedicated
   new test) asserting `history::load_or_default` for the sender's pubkey now returns exactly one entry
   matching the intro's `id`/`body`, `dir: In`, `state: Received`, `stream: "mrd.chat/1"` — failing before
   the fix, passing after.
6. A screen-level regression test (natural site: `apps/tui/tests/accept_to_chat.rs`) proving the two
   writers agree on `mid`, not just that each writes something: accept a request, open the resulting
   `Screen::Chat` (dispatching `Effect::LoadHistory`), and assert the intro appears **exactly once** in
   the rendered transcript — proving `apply_loaded_history`'s existing dedup-by-`mid` (4.44) genuinely
   coalesces this new writer's entry with anything already in memory, rather than merely asserting the
   file on disk is correct in isolation.
7. A restart-parity assertion (extend an existing restart-restore test, e.g. in
   `apps/tui/tests/live_session_file_backed.rs` or `accept_to_chat.rs` — implementer's call, documented):
   after accept + restart, the intro is present in the reloaded transcript, in the correct position
   relative to any reply.
8. `docs/architecture/tui-client.md` §11: an "Update (task 4.49)" paragraph appended after the existing
   "Update (task 4.46)" paragraph (§10/§11's prior text stays frozen, per the established convention).
9. `docs/tasks/phase-4/README.md`'s T17 exit-criteria bullet updated to record this defect as closed.
   **The checkbox itself stays unflipped** — that's task [4.50](./4.50-t17-acceptance-demo-closure-attempt-6.md)'s job, exactly as every prior wave left it for its own exit-gate task.

## Risks / notes
- **A narrower, but real, partial-failure window survives, named honestly rather than fixed.** The
  history write is placed *after* `save_trust`/`contacts::save` succeed, gated on `accepted` only. If
  `history::append` itself then fails, the function returns `Err`, and a client retry finds
  `accepted == false && pin_still_owed == false` (the pin is already complete) — the same early
  `Ok(None)` return the existing guard already takes for a fully-completed retry, per its own doc
  comment's derivation. The retry never re-attempts the history write: the peer stays durably trusted and
  fully reachable in the UI, but the intro is then permanently missing, even though nothing else about
  the accept is broken. **Do not widen the guard to cover this** — same "do not resurrect a case that
  should not self-heal automatically" discipline the function's own existing doc comment already states
  for the analogous `contacts.json`-row-missing case. Name this explicitly as a narrower analog of that
  already-tracked repair-action follow-up (`docs/tasks/phase-4/README.md`'s "Findings with no task yet")
  rather than opening a third, freestanding partial-failure task; the review round should confirm whether
  to fold it into that follow-up's own description.
- **`ChatContent::Receipt` as a first-contact intro is a degenerate, not a normal, case** — handle it
  type-safely (skip, don't error, don't invent), mirroring `intro_summary`'s existing precedent, rather
  than treating it as unreachable and `unwrap`-ing or panicking.
- **`chat_state_lock` scope is unchanged.** The history write is a separate sealed document from
  `sessions.bin`/`trust.bin`/`contacts.json`, not covered by that mutex — mirrors how
  `handle_persist_history`'s existing `Effect::PersistHistory` handler does not take that lock either. Do
  not introduce a new lock and do not widen `chat_state_lock`'s existing scope to cover it.
- **`ts` is accept-time, not original-request-arrival-time** — `MessageRequest` carries no arrival
  timestamp anywhere, so there is nothing more precise to source from. This is a deliberate, stated
  approximation (not a `TODO: confirm` — no design doc claims otherwise, and every other worker-side
  write in this crate already timestamps at write-time, not at an idealized original-event time).
- **Do not touch `TrustStore::can_send`'s fail-open semantics, `verification-ux.md`'s wording, or any
  trust-state transition** — this is a history-write gap, not a trust decision; unrelated to this fix
  entirely, restated only as a guardrail.
- **Definition of Done gate 9:** no new TUI surface — this repairs existing T17 core write path
  (Requests → accept → history), the same "intrinsically satisfied" reasoning 4.42/4.44/4.46 already
  used. No `SurfaceRegistry` registration is added.

## Tests
`cargo test -p meridian-tui --test run_worker_trust` (extended) · `cargo test -p meridian-tui --test
accept_to_chat` (extended) · `cargo test -p meridian-tui --test live_session_e2e --test
live_session_file_backed` · `cargo test -p meridian-tui --test history_load --test screen_main --test
screens_chat` (4.44's own targets — confirm untouched and still green) · `cargo test -p meridian-tui
--all-targets --no-fail-fast` · `cargo test -p meridian-cli --bins` · `bash harnesses/mitm-sim/run.sh`
(T08 regression — this task touches the accept path) · `bash tools/lint-tui-no-cli.sh` · `cargo fmt -p
meridian-tui -- --check` · `cargo clippy -p meridian-tui --all-targets -- -D warnings` · `bash
tools/check-docs.sh`.

## Reviews
- **reviewer** (combined). **architect** confirms the shape-1-over-shape-2 reasoning above holds once the
  real diff exists (a light-touch confirmation, not a load-bearing pre-code gate — this planning pass
  already resolved the choice, mirroring 4.46's own precedent of recording rather than deferring the
  assessment); **security-reviewer** owns "no new trust decision, plaintext history residency stays
  consistent with 4.44's own redaction precedent, nothing sealed incorrectly, no softened guard";
  **code-reviewer** owns the write ordering/placement, the narrowed partial-failure window's honesty, and
  the `ChatContent::Receipt` skip path.
- **test-engineer** — owns the two-writers-agree-on-`mid` coverage (Deliverable 6), the restart-parity
  assertion (Deliverable 7), and confirms `live_session_e2e.rs`'s workaround removal/conversion doesn't
  silently drop coverage.

## Links
- Feature spec: [17-terminal-tui-client.md](../../architecture/features/17-terminal-tui-client.md)
  (acceptance criteria: "restart restores contacts, history, and ratchet state … local dedup by message
  id")
- Design: [tui-client.md §11](../../architecture/tui-client.md#11-current-implementation-status-as-of-task-438--the-phases-second-exit-gate-attempt)
  (to receive this task's update paragraph) · [tui-client.md §5](../../architecture/tui-client.md#5-local-store--configuration)
  (`history.jsonl` schema)
- The finding this task exists to fix: [4.48's own Status section](./4.48-t17-acceptance-demo-closure-attempt-5.md#the-defect-the-accepted-senders-intro-message-is-silently-permanently-dropped-from-the-responders-own-history)
- Prior art: [4.44](./4.44-chat-history-load-on-open.md) (the reader half / dedup-by-`mid` contract this
  task's writer must agree with) · [4.42](./4.42-post-accept-chat-affordance.md) (Shape A —
  `run_accept_request`'s existing multi-document-write pattern this task extends by one write; also first
  deferred this exact question) · [4.46](./4.46-add-contact-trust-reconciliation.md) (most recent
  "no pre-code consult needed, reuse existing mechanism" precedent this task mirrors) ·
  task 4.32 (owns `run_accept_request`) · task 4.33 (owns `Effect::PersistHistory`/`run_persist_history`,
  the writer this task's call reuses)
- ADRs: [ADR 0021](../../adr/0021-client-local-store-config-formats.md) (the sealed-write rule this
  fourth write follows, same as the existing three)
- PR: <filled at /next-task time>

## Status
**Done.** Closes the fifth defect the phase's exit gate found (task 4.48): `worker::run_accept_request`
now persists the accepted sender's intro into the responder's `history.jsonl`, restoring T17's own
acceptance criterion "restart restores contacts, history, and ratchet state … local dedup by message
id" for the one message class that was silently, permanently lost.

**The fix.** `run_accept_request` now captures the real `Option<MessageRequest>` `chat.accept_request`
returns instead of collapsing it to `.is_some()`. On the genuine fresh-accept branch only (never the
`pin_still_owed`-only retry branch, which correctly has no `MessageRequest` to source content from),
after the existing `trust.bin`/`contacts.json` writes succeed: if the intro is `ChatContent::Text { id,
body }`, a `HistoryEntry` is built field-for-field matching what the ordinary inbound-Text handler
(`process_inbound_delivery`) already builds — same `mid: hex::encode(id)`, same `stream`, same
`state: Received` — and persisted via `crate::store::history::append`, the same writer
`run_persist_history` already uses. A `ChatContent::Receipt` intro is skipped, never invented into an
entry. Exactly the shape the plan-time "no pre-code consult needed" assessment predicted — nothing in
implementation diverged from it.

**The narrowed partial-failure window** (a failed history write after a successful accept leaves the
peer trusted but the intro permanently missing, with no retry re-attempting it) is documented honestly
in `run_accept_request`'s own doc comment rather than fixed — per the task's explicit instruction not to
widen the existing guard. `chat_state_lock`'s scope is unchanged; the history write is a separate sealed
document, mirroring `handle_persist_history`'s own precedent.

**`AcceptRequestRequest`'s doc comment** (`app.rs`) replaces the "deferred on purpose" paragraph 4.42
wrote with one recording this task's resolution, citing both 4.44 (the reader) and 4.49 (the writer).

**The `live_session_e2e.rs` workaround was removed, not converted** — its hand-rolled `persist_entry(...)`
call after `Effect::AcceptRequest` in `run_responder` is now redundant, since production performs the
equivalent write; the `run_initiator` call site was correctly left alone (it persists the live-inbound
*reply*, a path `run_accept_request` never touches). Review-round tracing confirmed this removal dropped
no real coverage: the file's own restart check was presence-only and never asserted the intro's specific
`mid`/`body` on either side, before or after this task — the workaround was never actually load-bearing
for what the file checks. Precise content/dedup coverage now correctly lives at `run_worker_trust.rs`
(raw disk write) and `accept_to_chat.rs` (screen-level round trip + restart) — a net improvement, not a
regression, at the worker-integration level.

**A genuine knock-on fix, not a design choice**, in `inbound_delivery.rs`:
`establish_accepted_conversation`'s own `Effect::AcceptRequest` now also writes the intro as an expected
transitive consequence of this fix, so `inbound_text_message_while_chat_open_appends_live_and_persists`'s
`saved.len()` assertion needed updating from `1` to `2` (intro + the test's own second message).
Independently traced and confirmed correct by both review passes — content-checked (`dir`/`body` per
entry), not a loosened count.

### Review round

`reviewer` (combined) — **architect: PASS**, shape-1 (inline write, existing writer, no new mechanism)
confirmed exactly as plan-time reasoning predicted, `chat_state_lock` scope confirmed unchanged;
**security-reviewer: PASS**, no trust-state transition touched, no new logging, `history.jsonl` sealed
through the same path every other writer uses; **code-reviewer: PASS**, retry-branch gating, the
`ChatContent::Receipt` skip, and the honestly-documented partial-failure window all verified from source.
One should-fix raised and resolved before closing: a single, non-reproducible test flake
(`history.len() == 0` instead of `1`) observed once in ~15 runs during review — independently
stress-tested afterward (20× isolated, 4× the exact combined-binary invocation the reviewer used, one
run cut off by a tool timeout with no failure observed) with zero reproductions; treated as transient
sandbox noise, not a defect this diff introduced, and not blocking.

`test-engineer` — **PASS with one should-fix, since closed**: independently confirmed the fix is real via
its own mutation testing (reverting the core write made all four directly-affected tests fail with the
expected symptoms, including a side finding that `live_session_e2e.rs` itself doesn't fail even with the
fix reverted — corroborating the workaround-removal analysis above, not a new gap). Found the
two-writers-agree-on-`mid` test (`accept_to_chat.rs`) didn't actually exercise `chat::insert_deduped` —
a freshly-opened `Screen::Chat` starts with `entries: Vec::new()`, so the disk-loaded entry had nothing
to dedup against, and the test's own "not twice" claim was true only because nothing could make it
false. **Closed**: the test now races a live, in-memory duplicate of the same intro (`AppEvent::Inbound`
with a matching `mid`) into `ChatState::entries` before completing the `Effect::LoadHistory` round trip,
mirroring `inbound_delivery.rs::a_racing_outbound_persist_history_dedups_against_a_same_mid_inbound_message`'s
existing idiom. Falsified before trusting it: reverting `apply_loaded_history`'s dedup to a naive concat
made the strengthened test fail (`left: 2, right: 1`) exactly as expected; restored and reran clean.
`tui-client.md`'s matching overclaim corrected alongside it. Restart-parity test and the
`inbound_delivery.rs` knock-on fix both independently confirmed genuine, not vacuous.

### Definition of Done
No wire/protocol/crypto change. No new dependency. `TrustStore::can_send`'s fail-open semantics,
`verification-ux.md`'s wording, and every trust-state-transition file left byte-for-byte untouched. Gate
9 (TUI surface) satisfied intrinsically — repairs an existing T17 core write path, no `SurfaceRegistry`
registration added. Full suite green throughout, including after the review-round test strengthening:
`cargo test -p meridian-tui --all-targets --no-fail-fast` (every target, 0 failures), `cargo test -p
meridian-cli --bins`, `bash harnesses/mitm-sim/run.sh`, `bash tools/lint-tui-no-cli.sh`, `cargo fmt -p
meridian-tui -- --check`, `cargo clippy -p meridian-tui --all-targets -- -D warnings`, `bash
tools/check-docs.sh` (2608 links, 0 broken). Docs synced: `tui-client.md` §11's "Update (task 4.49)"
paragraph (prior text frozen), this phase's README exit-criteria bullet. **Exit-criteria checkbox
deliberately left unflipped** — that is [4.50](./4.50-t17-acceptance-demo-closure-attempt-6.md)'s job.
