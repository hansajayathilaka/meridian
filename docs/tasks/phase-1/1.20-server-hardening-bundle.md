<!-- Fix-task for Phase 1 review finding F21 (nit; report says bundle as one task). -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report finding F21](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.20 — Server-hardening bundle (three small items)

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F21) · **Depends on:** — · **Status:** pending

## Goal
Three small server-hardening items land together, closing the nit-level gaps the report grouped as one
task: a structural server-no-core check, a salted-hash log-id helper ahead of observability, and bounded
rate-limiter growth.

## Scope
- **In:**
  - Convert `tools/lint-server-no-core.sh` from a grep into a `cargo tree` check covering
    identity/store/crypto **and transitive** routes (so the server-never-depends-on-core invariant is
    structurally enforced).
  - Add a salted-hash `LogId` helper + a tracing lint **before** observability lands (invariant #4
    currently holds only because the server logs nothing — this keeps it true once it does).
  - Evict expired rate-limiter entries at `apps/rendezvous/src/ratelimit.rs:35` (today growth is unbounded,
    keyed by attacker-controlled IPs).
- **Out:** the full observability/logging stack; the metrics/no-serde lints (→ 1.9/1.10).

## Deliverables
1. `tools/lint-server-no-core.sh` as a `cargo tree`-based check (+ transitive coverage).
2. A salted-hash `LogId` helper + a tracing lint guarding raw-id logging.
3. Rate-limiter expiry eviction in `apps/rendezvous/src/ratelimit.rs` + a growth-bound test.

## Risks / notes
Nit-severity — none urgent individually, bundled per the report. The `cargo tree` check must catch
transitive dependencies on core crates, not just direct. `LogId` must be salted (not a bare hash) so
identifiers aren't dictionary-recoverable from logs. Eviction must not drop still-valid limiter state.

## Tests
`bash tools/lint-server-no-core.sh` · `cargo nextest run -p meridian-rendezvous` (rate-limiter eviction +
LogId).

## Reviews
**security-reviewer** (invariant #4 + LogId salting), **code-reviewer** (eviction correctness).

## Links
- Report: [F21](./review-report.md)
- Skill: [anonymity-model](../../../.claude/skills/anonymity-model/SKILL.md)
- PR: <filled at /next-task time>
