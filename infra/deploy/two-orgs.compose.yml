# infra/deploy/two-orgs.compose.yml — SUPERSEDED (task 2.11).
#
# This file was the pre-2.11 scaffold stub for the two-org federation demo (feature 06):
# image placeholders, no CA/TLS wiring, a `TODO: confirm ports, static federation map contents,
# and TLS wiring` note. The real, runnable demo the feature spec and task tracker ask for
# (`demo/two-orgs/` — see the task's Deliverables) now lives at:
#
#   demo/two-orgs/docker-compose.yml (+ docker-compose.static.yml / docker-compose.srv.yml)
#
# Use that instead: `just two-orgs` (or `bash demo/two-orgs/run-walkthrough.sh [static|srv]`).
# See demo/two-orgs/README.md for the full walkthrough, both discovery modes, and the "why here,
# not there" note in the task file's Outcome section
# (docs/tasks/phase-2/2.11-demo-two-orgs.md).
#
# This file is kept only as a historical pointer (a few older references may still link to it) and
# is NOT maintained — it must never be run, and must never be allowed to silently drift from
# demo/two-orgs/'s real config. It carries no compose `services:` on purpose so `docker compose -f
# infra/deploy/two-orgs.compose.yml up` fails loudly instead of booting a stale, unwired stack.
