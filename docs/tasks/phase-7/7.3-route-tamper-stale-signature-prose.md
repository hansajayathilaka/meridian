> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/) · [Definition of Done](../../../CONTRIBUTING.md)

# 7.3 — Stale v1-signature prose in `route_tamper.rs`

**Phase:** 7 — Review of Phase 6 · **Feature:** review fix (F5) · **Depends on:** — · **Status:** done

## Goal
Fix the two body-comment sites inside `apps/rendezvous/src/route_tamper.rs` that task 6.7's own
`/plan-phase` refinement #3 named as in-scope but missed — the file's module-level doc was correctly
rewritten, these two weren't.

## Scope
- **In:** `apps/rendezvous/src/route_tamper.rs`, comment-only:
  - ~line 136: "the receiver compares `Deliver.from` against the *signed* `sender_pub` inside the
    envelope" → reword to AEAD-authenticated-plaintext-field language (mutation is caught by the ratchet
    AEAD tag on decrypt, not a signature check; `sender_pub` is an unauthenticated-at-the-server
    plaintext field the *client* checks post-decrypt) — matching the fix already applied to this file's
    module doc and to `apps/cli/tests/relay_attacks.rs`.
  - ~line 151: "valid, correctly signed, but belonging to a *different* session" → drop "correctly
    signed"; describe as a genuine, AEAD-valid envelope belonging to a different session.
- **Out:** No `.rs` logic changes — `plan()`'s actual byte-field comparisons are unaffected; confirmed no
  behavior/security impact by the architect review that found this gap.

## Deliverables
1. The two comment edits.

## Risks / notes
None — comment-only, no security-relevant logic touched.

## Tests
`cargo build -p meridian-rendezvous && cargo clippy -p meridian-rendezvous --all-targets -- -D warnings`
(proves the crate still builds clean; no behavioral test applies).

## Reviews
**code-reviewer** only — comment-only, no security-relevant logic touched, and the near-identical fix
already got a combined-reviewer pass in 6.7 for the rest of this same file.

## Links
- Review finding: [phase-7/review-report.md](./review-report.md) F5
- Precedent: [phase-6/6.7-doc-sync-envelope-v2.md](../phase-6/6.7-doc-sync-envelope-v2.md)
- PR: <filled at /next-task time>

## Status
**Done.** Both comment sites reworded to describe `sender_pub` as an AEAD-authenticated plaintext
field (recovered only on a successful ratchet decrypt), matching the framing already used by this
file's module doc and by `apps/cli/tests/relay_attacks.rs` (both fixed by 6.7). No logic changed —
confirmed comment-only. `cargo build -p meridian-rendezvous` and `cargo clippy -p meridian-rendezvous
--all-targets -- -D warnings` clean.

### Review round
**code-reviewer** — **PASS**, no findings. Confirmed the new wording is accurate, the diff is
comment-only, the file now reads consistently end-to-end with 6.7's fixes elsewhere, and a full-file
scan found no other stale "signed"/"signature" language remaining in this file.

### Definition of Done
Doc/comment-only change, no wire/protocol/crypto behavior change, no new dependency. Build + clippy
clean. Gate 9: N/A.
