# coturn config (scaffold stub). See ../../docs/operations/deployment.md.
# TURN credentials are ephemeral HMAC tokens minted per session by the rendezvous —
# do NOT put static long-term secrets here in production.
#
# use-auth-secret
# static-auth-secret=<TODO: confirm — provisioned out of band, never committed>
# realm=<TODO: org domain>
# listening-port=3478
# tls-listening-port=5349
# no-cli
# TODO: air-gapped mode uses internal STUN/TURN only; disable external relays.
