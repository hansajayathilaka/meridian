<!-- Copy this file to docs/tasks/phase-N/P.N-<slug>.md and fill every section.
     Shape mirrors the feature specs in docs/architecture/features/. -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [ADR 0025](../../adr/0025-ice-restart-renegotiation.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 10.21 — Tolerant (mailbox-eligible) restart delivery

**Phase:** 10 — File Transfer Stream · **Feature:** T09 (fix) · **Depends on:** 10.20 ·
**Status:** done

## Goal
Give `SignalRelay` a way to send an envelope tolerantly — treating "queued to the peer's mailbox"
(T07/[ADR 0007](../../adr/0007-offline-mailbox.md)) as a normal, non-error outcome rather than the
existing hard-fail `SignalRelay::send`'s "peer is not currently connected" error — per
[ADR 0025](../../adr/0025-ice-restart-renegotiation.md)'s decision that restart signaling must not
require holding a relay connection open for a session's full lifetime.

## Scope
- **In:** `apps/core/src/session.rs`'s `SignalRelay` trait — add a new method (e.g.
  `send_tolerant(&mut self, to: &[u8; 32], blob: Vec<u8>) -> Result<RouteOutcome, SessionError>`, name
  and exact shape at the implementer's discretion, but it must surface "delivered live" vs. "queued to
  mailbox" as distinct, non-error outcomes and only error on a genuine failure) alongside the existing
  hard-fail `send`, which stays unchanged for the original dial/answer contract (ADR 0025's Option B
  rejection explains why that contract must stay hard-fail). `RendezvousRelay`
  (`apps/core/src/signal_relay.rs`) implements it via `SignalingClient::route_with_hint_detailed`
  (already built for T07, returns exactly `RouteOutcome{delivered, queued}` — do not duplicate this
  logic, call through to it). `MemRelay` (the in-process test double, also in `session.rs`) needs a
  reasonable tolerant-send simulation too, since it's what most of this phase's own tests construct
  sessions against — check how it currently models delivery and extend it consistently (e.g. it may
  already always "deliver," in which case tolerant-send trivially always returns `delivered: true`;
  confirm rather than assume).
- **Out:** anything that actually calls this new method with a real `IceRestartOffer`/`IceRestartAnswer`
  payload (that's [10.22](./10.22-session-ice-restart-signaling.md)'s job) — this task only builds the
  relay-level primitive and proves it round-trips both outcomes (delivered vs. queued) in isolation,
  with a placeholder/arbitrary blob if needed for the test.

## Deliverables
1. `SignalRelay`'s new tolerant-send method + `RendezvousRelay`/`MemRelay` implementations.
2. Tests proving both outcomes: a live peer receives it immediately (`delivered: true`), an offline
   peer's send returns `queued: true` without erroring (mirror whatever the existing `meridian-rendezvous`
   mailbox test harness already uses to put a peer "offline" for T07's own tests — don't invent a new
   offline-simulation mechanism).

## Risks / notes
Don't touch `RendezvousRelay::send`/`map_route_result`'s existing hard-fail behavior — it's correct for
the dial/answer contract this task doesn't touch (ADR 0025 is explicit that Option B, reusing that
contract, was rejected specifically because it's *supposed* to stay hard-fail for a fresh dial). This
task adds a sibling method, not a behavior change to the existing one.

## Tests
`cargo nextest run -p meridian-core` (new tolerant-send tests, both outcomes); existing suite stays
green.

## Outcome
Added `SignalRelay::send_tolerant(&mut self, to, blob) -> Result<RouteOutcome, SessionError>`
(`apps/core/src/session.rs`) — `RouteOutcome` is `meridian_signaling::RouteOutcome`, already reachable
through `meridian-core`'s existing `pub use meridian_signaling as signaling` re-export (already used
directly as a public-boundary type elsewhere, e.g. `apps/tui/src/worker.rs`, `apps/cli/src/chat.rs`
from task 8.15), so no wrapper type or new dependency edge was needed. `RendezvousRelay::send_tolerant`
(`apps/core/src/signal_relay.rs`) thinly wraps `SignalingClient::route_with_hint_detailed` (T07's
existing primitive) via a new `map_route_outcome_result`, passing both `delivered` and `queued`
through as success and only a genuine `SignalError` as failure. `MemRelay::send_tolerant` reuses the
exact same dropped-receiver-half `SessionError::SignalingEnded` failure mode the existing `send`
already uses — no new offline-simulation mechanism invented. The "queued to mailbox" outcome is
proven via `apps/rendezvous/tests/rendezvous.rs::route_to_offline_peer_with_mailbox_enabled_queues`
(a real spawned rendezvous, a peer that never connects, `RouteOutcome{delivered:false,queued:true}`)
as the real-server ground truth, since `route_with_hint_detailed`'s own mapping is a trivial
pass-through with no logic of its own worth a second integration test.

Two other `SignalRelay` implementors, outside this task's original file list but required for the
trait addition to compile, were updated minimally and consistently: `CountingRelay`
(`apps/core/tests/p2p_session.rs`, delegates to its inner `MemRelay`) and both `FileRelay` structs in
task 10.15's example drivers (`apps/cli/examples/{kill_resume_netns_drive,netns_ice_restart_probe}.rs`,
delegate to their existing `send`) — confirmed neither example calls `send_tolerant` anywhere yet, so
task 10.15's own kill/resume scenario is unaffected by this addition.

The combined **reviewer** pass returned **PASS on all three lenses, zero blocking findings** —
independently confirmed the existing hard-fail `send`/`map_route_result` are genuinely untouched (only
additions in the diff), traced the `RouteOutcome` re-export and judged it consistent with existing
precedent rather than an abstraction leak, verified the dropped-receiver failure-mode reuse claim by
reading the actual `SessionError::SignalingEnded` usage, and read the cited rendezvous test directly to
confirm it's genuine ground truth for the queued-outcome shape rather than an evasive shortcut.

### Commands run
`cargo nextest run -p meridian-core` — 164/164 (5 new); `cargo nextest run -p meridian-cli --features
webrtc` — 98/98; `cargo clippy --workspace --all-targets [--features webrtc] -- -D warnings` (both
configs) — clean; `cargo fmt --all -- --check` — clean; `cargo build --workspace [--features webrtc]`
(both configs) — succeed.

## Reviews
**reviewer** (combined: architect — extends a session-substrate trait per ADR 0025's decision;
code-reviewer).

## Links
- ADR: [0025](../../adr/0025-ice-restart-renegotiation.md)
- PR: [#92](https://github.com/hansajayathilaka/meridian/pull/92)
