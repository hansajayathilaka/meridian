<!-- Fix-task for Phase 1 review finding F19. -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report finding F19](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.4 — Repair the corrupted roadmap "Phasing" section + ADR 0013 tail artifact

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F19) · **Depends on:** — · **Status:** done

## Goal
The roadmap's "Phasing" section reads correctly again, and the botched-extraction artifacts are gone —
so the docs are not silently corrupted (the docs CI only checks links + mermaid, so this class of damage
is invisible to it).

## Scope
- **In:** restore `docs/architecture/roadmap.md:30-43` ("Phasing" section) from the intact source at
  `docs/architecture/system-design.md:317-325`; remove the spliced-in ADR fragment. Clean up the smaller
  artifact of the same botched extraction at the tail of `docs/adr/0013-server-web-framework.md`.
- **Out:** changing the docs CI to catch this class of corruption (noted as a separate concern; out of
  scope here).

## Deliverables
1. Repaired `docs/architecture/roadmap.md` "Phasing" section.
2. Cleaned `docs/adr/0013-server-web-framework.md` (tail artifact removed).

## Risks / notes
Restore content faithfully from `system-design.md:317-325` — do not paraphrase or re-derive the phasing.
Confirm the ADR 0013 body (the axum decision) is intact after removing the tail fragment.

## Tests
`diff` the restored "Phasing" text against `system-design.md:317-325`; docs CI link/mermaid check.

## Reviews
**code-reviewer** (diff correctness), **architect** (content fidelity to the canonical source).

## Links
- Report: [F19](./review-report.md)
- Source of truth: `docs/architecture/system-design.md:317-325`
- PR: opened on branch `claude/next-task-cn1pwx`

## Result
Found `docs/architecture/roadmap.md`'s "Phasing" section had already been rewritten as a paraphrased
summary table rather than the ADR-spliced corruption the finding described — but a paraphrase still
violates the task's "restore faithfully, do not paraphrase or re-derive" requirement. Replaced the
table with the verbatim "Phased roadmap" prose from `docs/architecture/system-design.md:317-325`
(five `**Phase N — …:**` paragraphs + the "Sequencing rationale" sentence), confirmed byte-for-byte
identical via `diff`. Removed the stray truncated heading ("## 7. What a new engineer runs on day
one") spliced onto the tail of `docs/adr/0013-server-web-framework.md`; confirmed the ADR's actual
body (the axum decision) is fully intact and the file now ends cleanly. `tools/check-docs.sh` passes
(1001 links, no broken links). Reviewed by **code-reviewer** (PASS — diff-identical, ADR body intact,
no other corruption artifacts found nearby) and **architect** (PASS — roadmap.md correctly defers to
system-design.md as canonical source rather than re-deriving it, no ADR contradictions introduced,
the execution-vs-design-phase disambiguation note is intact; flagged one pre-existing, out-of-scope
nit — `roadmap.md:3`'s source comment still cites the old `p2p-comms-design.md` filename, not touched
here). No code changed.
