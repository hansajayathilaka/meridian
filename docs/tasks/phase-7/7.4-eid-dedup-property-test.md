> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/) · [Definition of Done](../../../CONTRIBUTING.md)

# 7.4 — Property test for `eid` dedup bound + duplicate detection

**Phase:** 7 — Review of Phase 6 · **Feature:** review fix (F6) · **Depends on:** — · **Status:** done

## Goal
Add randomized/property coverage alongside the existing three example-based cells in
`apps/core/tests/eid_dedup.rs`, so the dedup bound and duplicate-detection invariants hold across
generated sequences, not just the three hand-picked scenarios.

## Scope
- **In:**
  - `apps/core/Cargo.toml` — add `proptest = { workspace = true }` to `[dev-dependencies]` (already
    pinned at the workspace root, `Cargo.toml:84`, and already used by `apps/identity` — wiring an
    already-vetted dependency into a new crate's dev-deps, not introducing a new one).
  - `apps/core/tests/eid_dedup.rs` — a new property-test block covering:
    1. Exact-duplicate detection regardless of arrival order: a random interleaving of genuinely fresh
       envelopes and re-deliveries of already-delivered ones; every re-delivery must be
       `Err(ChatError::DuplicateEnvelope)`, every fresh one must succeed, in any interleaving.
    2. `seen_eids_len(&state) <= MAX_SEEN_EIDS` as an invariant across randomized sequence lengths (the
       exact-at-cap boundary is already covered by the existing
       `seen_eid_set_is_bounded_under_a_flood_of_distinct_eids` cell; this property generalizes across
       smaller random lengths/duplicate rates rather than re-proving that same boundary).
- **Out:** Growing `seen_eids` costs one genuine X3DH handshake per new entry (by design, per 6.4's
  Decision 3 — an attacker cannot grow it for free). Keep case count and per-case sequence length small
  (e.g. 8–16 cases, sequences bounded to a few dozen entries) so total handshake count stays well under
  `MAX_SEEN_EIDS` and runtime stays in the single-digit-seconds range — do not attempt a full
  `MAX_SEEN_EIDS`-sized flood inside every generated case.

## Deliverables
1. The `Cargo.toml` dev-dependency addition.
2. The new property test(s) in `eid_dedup.rs`, additive only — the three existing cells are unchanged.

## Risks / notes
Non-vacuity matters here: confirm each property actually fails when its invariant is broken (revert the
dedup check / cap logic locally and confirm red), the same discipline 6.4's own review applied — don't
just trust the property compiles and passes.

## Tests
`cargo nextest run -p meridian-core --test eid_dedup`.

## Reviews
**test-engineer** (mandatory — non-vacuity of the property) + **code-reviewer**.

## Links
- Review finding: [phase-7/review-report.md](./review-report.md) F6
- Precedent: [phase-6/6.4-eid-replay-dedup.md](../phase-6/6.4-eid-replay-dedup.md)
- PR: <filled at /next-task time>

## Status
**Done.** Added `proptest = { workspace = true }` to `apps/core/Cargo.toml`'s dev-dependencies
(matching `apps/identity`'s existing convention) and a new `eid_dedup_holds_under_random_interleavings`
property test (12 cases) to `apps/core/tests/eid_dedup.rs`, generalizing two of task 6.4's claims
across randomized sequences: exact-duplicate detection regardless of arrival order (any
already-delivered index rejected as `ChatError::DuplicateEnvelope`, any fresh one accepted), and
`seen_eids_len(&state) <= MAX_SEEN_EIDS` tracked exactly below the cap. A new
`confirmed_session_with_envelopes(n)` helper does exactly one X3DH handshake per generated case, then
seals `n` further steady-state envelopes cheaply, keeping total handshake count small per the task's
cost-discipline constraint. Existing three cells unchanged.

**Non-vacuity, independently verified twice** (implementer, then test-engineer from scratch):
neutralizing the dedup guard in `open_bytes` (`apps/core/src/chat.rs:1675`) makes the new property
fail with a genuine, meaningful assertion (a replayed envelope reaches the ratchet decrypt path a
second time and fails as `Err(Crypto(Crypto))` instead of being short-circuited as a duplicate) —
proptest's shrinker reduced it to a minimal 7-step counterexample. Reverted both times; production
code is untouched in the final diff.

### Review round
**code-reviewer** — **PASS**, no blocking/should-fix findings. Confirmed `confirmed_session_with_envelopes`
matches the file's existing sealing/receiving pattern, each envelope's `eid` is independently
fresh-random so genuinely distinct, and out-of-order delivery stays far inside the ratchet's
`MAX_SKIP`/`MAX_SKIPPED_STORED` window (1000/2000 vs. this test's ≤24-step sequences) — no spurious
`ChatError::Crypto` rejection is reachable, and the assertions would loudly fail rather than mask one
if it were. `Cargo.lock`/`Cargo.toml` hygiene confirmed minimal and consistent.

**test-engineer** — **PASS**. Independently re-ran the falsifiability check from scratch (see above),
confirmed proptest's shrinking is live (not disabled) with a genuinely minimal counterexample, measured
runtime (~1.5–1.8s for the whole file) and confirmed the full crate suite stays green.

*Note: both reviewer passes ran in parallel against the same shared working tree, and code-reviewer
transiently observed test-engineer's own in-flight falsifiability edit to `chat.rs` mid-review before
it was reverted — a benign race between two read-mostly review agents, not a defect in the diff.
Confirmed `chat.rs` clean and all tests green after both agents finished.*

### Definition of Done
No production code change. New dev-dependency (`proptest`, already workspace-pinned and precedented
by `apps/identity`) — not a new external dependency to the project. `cargo test -p meridian-core --test
eid_dedup` (4/4) and full `cargo test -p meridian-core` green. `cargo fmt -p meridian-core -- --check`
and `cargo clippy -p meridian-core --all-targets -- -D warnings` clean. Gate 9: N/A.
