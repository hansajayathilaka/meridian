<!-- Fix-task for Phase 1 review finding F10 (backend implementation) — the weeks-long task. -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report finding F10](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.15 — webrtc-rs `Transport` backend (replace the empty `webrtc = []` feature)

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F10 backend) · **Depends on:** 1.13, 1.3 · **Status:** done

## Goal
A real webrtc-rs `Transport` implementation exists behind the existing trait, so P2P, NAT traversal, and
relay-only IP-hiding are proven against real ICE/SCTP data channels instead of only `LoopbackTransport`.
This is the blocking Feature-4 gap and the one task that may span multiple PRs.

## Scope
- **In:** wire the `webrtc` dependency + gated code at `apps/transport/Cargo.toml:23` (today `webrtc = []`
  is an empty flag); implement `WebRtcTransport` satisfying the existing `Transport` trait — data channel
  create/accept, ICE, SCTP, DTLS fingerprint binding (fail-closed), the ctrl channel and stream registry
  wiring that already exist against loopback; make the headline demo ("kill the server, chat continues")
  run over a real connection in `apps/core`.
- **Out:** the netns/tcpdump acceptance matrix and observed-candidate relay-only classification (→ 1.16);
  coturn quota (→ 1.14); media/libwebrtc (Feature 10, ADR 0014).

## Deliverables
1. `WebRtcTransport` impl in `apps/transport` behind the (now non-empty) `webrtc` feature.
2. `apps/core` session wiring using the real backend for the P2P demo.
3. Gated integration tests (fingerprint binding fail-closed, ctrl channel, capability rejection, ICE
   restart) running over the real transport, mirroring the existing loopback tests.

## Risks / notes
`TODO: confirm` — if the `Transport` trait surface proves under-specified for a real backend, route the
gap to **architect** against ADR 0006/0014 rather than guessing a trait change. **May exceed one PR** —
`/next-task` may re-split at build time (e.g. plumbing vs. ICE vs. demo); keep the fail-closed fingerprint
binding invariant intact at every split. Must not regress the loopback path (keep it as the default/test
transport). Coordinate the transport-label change with 1.13 (merge 1.13 first).

## Tests
`cargo nextest run -p meridian-transport --features webrtc` · `cargo nextest run -p meridian-core --features webrtc` · the P2P demo over a real connection.

## Reviews
**architect** (Transport trait / ADR 0006/0014), **security-reviewer** (fingerprint binding, fail-closed),
**connectivity-debugger** (ICE/SCTP/DTLS), **test-engineer**.

## Status (done)
`WebRtcTransport` (`apps/transport/src/webrtc_backend.rs`) implements the full `Transport` trait over
webrtc-rs 0.17: real ICE gathering, `RTCIceTransportPolicy::Relay` for a genuine `relay-only` (never
gathers host/srflx), pre-negotiated SCTP data channels (avoids the in-band duplicate-channel footgun —
see the module's doc comment for the full design writeup), and fingerprint binding read from the
ratchet-encrypted-envelope-carried SDP text (real WebRTC/DTLS enforces cert-matches-SDP-fingerprint as
a hard precondition of the handshake ever completing — proven by
`apps/transport/tests/webrtc_backend.rs::tampered_remote_fingerprint_never_connects`). `meridian-core`
forwards a matching `webrtc` feature; `dial_with_config`/`answer_with_config` now close the transport
session on any failed setup path (a real backend leaks a live `RTCPeerConnection`, not just a HashMap
entry, if it doesn't) and fail fast on a connection that never converges instead of hanging forever on
an unbounded `recv()`.

Delivered as scoped, not full-scope:
- **ICE restart is a documented no-op on the wire**, not a real fix — see the module doc's "ICE
  restart does not (yet) fulfill the resumability promise" section. Calling webrtc-rs's real
  ICE-agent restart on an already-connected `RTCPeerConnection` with no peer-side signaling breaks
  the live connection (verified empirically); there is no ctrl-channel renegotiation path today to
  carry a restart offer to the peer. This means system-design §5.2/§7.3's "resumable... ICE restarts
  on network change" promise and feature-04's Wi-Fi→LTE acceptance criterion remain **unproven for
  real networks** after this task — flagged for architect review (successor to 1.15/1.16), do not
  ship network-roaming support claiming this works until it lands.
- **relay-transport classification (udp/tcp/tls-443) is not derivable** from webrtc-rs 0.17.1's stats
  API today (it hardcodes `relay_protocol: "udp"` upstream); `selected_path_detail` reports `Udp`
  unconditionally for any relay pair pending 1.16.
- The active relay-rewrite-attack test against a real backend (flagged by 1.13) is still open — a
  routing-only relay cannot tamper with the SDP without breaking the ratchet envelope's authenticity,
  so the meaningful adversarial test here is `tampered_remote_fingerprint_never_connects` (self-
  inconsistent envelope / malicious-peer property), not a relay-rewrite property; genuinely tracked
  for 1.16 if a different attack model turns out to need it.
- CLI `--transport webrtc` wiring was not added; "apps/core session wiring using the real backend for
  the P2P demo" (deliverable 2) is satisfied by
  `apps/core/tests/p2p_session_webrtc.rs::server_down_chat_continuity_over_webrtc`, a gated
  integration test proving the headline demo's exact behavior over a real connection.

Reviewed by architect, security-reviewer, connectivity-debugger, and test-engineer; all
blocking/actionable findings were fixed (session resource-leak-on-error-path, connect-timeout on
`recv()`, negotiated-channel-id collision guard, tampered-fingerprint test, CI/Justfile wiring for the
gated suite) or explicitly documented as known, scoped gaps above.

## Links
- Report: [F10](./review-report.md) (backend); honesty → 1.13; wire matrix → 1.16
- ADRs: 0006 (terminal transport), 0014 (media stack / data split)
- Skill: [webrtc-nat-traversal](../../../.claude/skills/webrtc-nat-traversal/SKILL.md)
- PR: <filled at /next-task time>
