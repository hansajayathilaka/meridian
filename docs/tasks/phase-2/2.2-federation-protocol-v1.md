> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/06-cross-org-federation.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 2.2 — `federation-protocol-v1.md` + s2s wire types + conformance vectors

**Phase:** 2 — Cross-Org Federation · **Feature:** T06 — Cross-Org Federation · **Depends on:** 2.1 · **Status:** done

## Goal
Publish the new versioned server-to-server wire contract and its `meridian-proto` types, so every
later task codes against a fixed, vector-checked contract instead of inventing one per PR.

## Scope
- **In:** the contract doc; s2s frame + body types in `meridian-proto`; conformance vectors and their
  generator; the `tools/lint-no-serde-on-blob.sh` allowlist extension as an explicit, reviewed line
  item; index/nav updates.
- **Out:** all behaviour — no listener, dialer, discovery, policy or handler (2.4–2.9). No c2s changes
  (2.3).

## Deliverables
1. `docs/api/federation-protocol-v1.md` — framing, ops, error codes, versioning, and the "what two
   federating orgs may learn" ceiling restated from
   [anonymity-and-retention.md](../../security/anonymity-and-retention.md).
2. `apps/proto/src/fed.rs`: `FedOp`, `FedFrame`, `FedHello`, `FedFetchBundle`, `FedBundle`,
   `FedRoute`, `FedReachability`, `FedReachable`, `FedErr`, `fed_error_codes`.
3. `test-vectors/federation-v1.json` + `tools/xtask/src/vectors/federation.rs`, registered in
   `tools/xtask/src/vectors/mod.rs::generate`.
4. `apps/proto/tests/fed_roundtrip.rs`.
5. Updates to [docs/api/README.md](../../api/README.md) and
   [wire-protocol.md §4](../../api/wire-protocol.md) pointing at the new doc as the authority.

## Risks / notes
- The origin-attestation shape is **decided by 2.1** — implement that decision, do not re-open it.
- `TODO: confirm`: s2s framing/transport. The docs say "mTLS" and name ops but never the framing —
  CBOR `{op,id,body}` over a length-delimited mTLS stream, a second axum WSS listener, or HTTP/2.
- `TODO: confirm`: `Frame.id` semantics for s2s. The c2s space is client-chosen with `id = 0` reserved
  for server-initiated frames; an s2s reuse needs its own id space.
- `TODO: confirm`: whether reusing the c2s `Op` enum lets a *client* socket nominally speak federation
  ops. The `serve` loop rejects unknown ops today, but a merged enum stops distinguishing the planes.
- [wire-protocol.md §4](../../api/wire-protocol.md)'s optional `contact_token{issuer_sig, audience,
  exp}` is documented, but contact tokens are explicitly **out of scope** (→ T08/T14). Record the
  field as reserved-and-unimplemented; do not implement it.
- Types must live in `meridian-proto` — it is the only placement that keeps
  `tools/lint-server-no-core.sh` green (the lint is a crate-**name** allowlist, so even a new
  `meridian-federation` crate would fail it without touching core).
- Extending `tools/lint-no-serde-on-blob.sh`'s type allowlist is intended friction: it must be a
  reviewed line item here, never a silent edit in a later task, and must never admit a content-shaped
  type.

## Tests
```
cargo nextest run -p meridian-proto
cargo run -p xtask -- vectors && git diff --exit-code test-vectors/
bash tools/lint-no-serde-on-blob.sh && bash tools/lint-no-serde-on-blob.sh --selftest
bash tools/check-docs.sh
```

## Reviews
- **architect** — required; a new versioned wire contract, and it implements 2.1's decision.
- **security-reviewer** — required; the opacity ceiling, no contact-graph fields, no plaintext.
- **code-reviewer**.

## Links
- Feature spec: [06-cross-org-federation.md](../../architecture/features/06-cross-org-federation.md)
- ADRs: [0002](../../adr/0002-federation-mechanism.md) · ADR 0017 (from task 2.1)
- Skill: [api-contracts](../../../.claude/skills/api-contracts/SKILL.md)
- PR: _filled at /next-task time_

## Status

**Done.** [`docs/api/federation-protocol-v1.md`](../../api/federation-protocol-v1.md) is published and
[`apps/proto/src/fed.rs`](../../../apps/proto/src/fed.rs) implements ADR 0017's decisions verbatim:

- **`FedRoute{to, from, envelope}`** — the C1/C2 canonical shape; `from` is routing metadata asserted
  by the sending server, never decoded from the opaque `envelope`.
- **`FedOp`/`FedFrame`** are a distinct plane from the c2s `Op`/`Frame` — structurally incapable of
  being confused, verified by grep against `apps/rendezvous/` (zero references).
- **Transport/framing decided, not left `TODO: confirm`**: length-delimited CBOR directly on the raw
  mTLS byte stream (no WS/HTTP2) — the simplest shape satisfying ADR 0017 C7's in-process-termination
  requirement. `id = 0` reserved for a two-way `FedHello` exchange; every other id is chosen by
  whichever side initiates (s2s has no client/server asymmetry to reserve a shared id space around).
- **`FedHello.domain`/`FedFetchBundle.requesting_server`** are documented in three independent places
  as self-asserted/informational only — the mTLS peer identity is the sole authoritative source for
  any policy/routing/rate-limit decision (C5).
- **`contact_token`** recorded as reserved-and-unimplemented (→ T08/T14); no corresponding field.
- `test-vectors/federation-v1.json` + `tools/xtask/src/vectors/federation.rs` cover all 7 body types
  deterministically; `apps/proto/tests/fed_roundtrip.rs` round-trips every type through `FedFrame`.
- `tools/lint-no-serde-on-blob.sh`'s `ALLOWLIST` extended with the 7 new control-plane type names as
  an explicit, reviewed line item (not a silent addition in a later task).
- Scope honored: zero diff in `apps/rendezvous/`; `wire-protocol.md §2`'s stale `deliver{from_server,
  blob}` duplicate deliberately left for [2.3](./2.3-c2s-federation-extension.md).

### Reviews
- **architect** — ✅ consistent, no revision needed. Verified C1/C2/C5/C7 against `fed.rs` and the doc,
  the dependency graph (`meridian-proto` only, no new crate, `apps/rendezvous/` untouched), and that
  the three previously-open design decisions (framing, `Frame.id` semantics, distinct `FedOp`) were
  resolved soundly.
- **security-reviewer** — ✅ APPROVE, no changes required. Verified opacity (every `fed.rs` field is a
  key/bool/short string/`OpaqueBlob`, nothing content-shaped), no contact-graph materialization, the
  "what two orgs may learn" ceiling accurately restates anonymity-and-retention.md, the self-asserted
  fields can't become a trust foothold, `from` attestation matches C1/C2 exactly, `contact_token` is
  genuinely absent, and the vectors use obviously-synthetic byte patterns.
- **code-reviewer** — ✅ Approve, no blocking findings. Independently re-ran every test/lint/doc
  command plus `lint-server-no-core.sh`, clippy, and fmt. Two non-blocking nits noted (one redundant
  test case, and b32 fields not getting `OpaqueBlob`'s dedicated compact-CBOR-shape unit assertion —
  covered indirectly by the generated vectors + CI's byte-identical gate); neither is required for this
  task's Definition of Done.

### Verified
`cargo test -p meridian-proto` (5/5 pass) · `cargo run -p xtask -- vectors && git diff --exit-code
test-vectors/` (clean) · `bash tools/lint-no-serde-on-blob.sh` + `--selftest` (pass) · `bash
tools/check-docs.sh` (1544 links, none broken) · `cargo clippy -p meridian-proto -p xtask --all-targets
-- -D warnings` + `cargo fmt --check` (clean) · `bash tools/lint-server-no-core.sh` (pass).
