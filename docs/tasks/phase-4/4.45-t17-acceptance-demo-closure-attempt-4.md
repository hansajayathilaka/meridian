> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/17-terminal-tui-client.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 4.45 — T17 acceptance-demo closure (fourth exit-gate attempt)

**Phase:** 4 — Verification & Trust + Terminal TUI Client · **Feature:** T17 — Terminal TUI Client (gap closure, phase re-exit gate) · **Depends on:** 4.42, 4.43, 4.44 · **Status:** pending

## Goal
Mirror 4.28's, 4.38's and 4.41's role a fourth time: run the T17 demo script live, end to end, for both
account types, and — **only if it genuinely passes** — flip Phase 4's exit-criteria checkbox and clear the
tracker's carry-forward note. This is the task that actually closes Phase 4, if 4.42, 4.43 and 4.44 did
what they claim.

## Scope
- **In:** a live PTY run of the **full** demo script from
  [17-terminal-tui-client.md](../../architecture/features/17-terminal-tui-client.md)'s "Working output",
  for a **file-backed** account and an **OS-keystore** account, covering every step no prior attempt
  reached:
  1. onboarding → registered → bundle published → `Screen::Main`, both account types;
  2. initiator adds the responder by `mrd1:` id with petname "bob" and sends a first-contact message;
  3. responder sees the message request (key + safety number + intro) and accepts;
  4. **responder opens a chat with the accepted sender and replies** (4.42's fix — the step 4.41 could
     not perform at all);
  5. **`^V`/`v` shows the same 60-digit safety number on both sides → mark verified** on both;
  6. **restart both peers** (`pkill` + relaunch) and confirm contact list, petnames, **full history** and
     the ratchet session are all restored with no re-handshake and no duplicated messages (4.44's fix);
  7. `meridian tui --export-json <path>` + `jq '.contacts[0].petname'` returns `"bob"`.
  Also: record the **file-backed session-start latency** observed here and compare it against 4.43's own
  recorded before/after numbers (4.41 measured 188.0 s / 188.8 s; a regression to minutes is a failure of
  this gate even if every functional step passes).
  Re-run the standing regression set every prior attempt held itself to: `cargo test -p meridian-tui
  --all-targets --no-fail-fast`, `cargo test -p meridian-cli --bins`, `cargo test -p meridian-tui --test
  live_session_e2e`, `bash harnesses/mitm-sim/run.sh` (T08 regression), `bash tools/lint-tui-no-cli.sh`,
  `bash tools/check-docs.sh`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt
  --check`.
  Hold to the **two-independent-runs discipline** 4.28/4.38/4.41 all used — implementer once,
  `reviewer`/`test-engineer` once, independently. **Not relaxed:** three exit-gate attempts in a row have
  each found a real defect this exact way.
- **Out:** any new feature code; fixing anything newly found live here. **If a genuine defect surfaces,
  report it honestly in this task's own Status section, do not fix it here, file a new task (working
  number 4.46), and leave both the exit-criteria checkbox and the carry-forward note open** — the same
  non-negotiable rule 4.28, 4.38 and 4.41 all held. Never weaken an assertion (this task's own, or
  4.42's/4.43's/4.44's) to make the demo appear to pass.

## Deliverables
1. This task file's own **Status section** with live-run evidence (condensed chronological transcripts,
   command output, timings) at the same evidentiary bar 4.28/4.38/4.41 set — including an explicit, named
   statement of anything that had to be narrowed and why.
2. **Only if the demo genuinely passes:** a new `docs/architecture/tui-client.md` §12 documenting this
   attempt (§10/§11 stay frozen as historical record, per the existing convention);
   `docs/tasks/phase-4/README.md`'s T17 exit-criteria checkbox flipped; `docs/tasks/README.md`'s
   carry-forward note cleared and ▶ NOW/NEXT advanced to `/start-review-phase` for Phase 5.
3. Any residual coverage gap not closed by 4.42's/4.43's/4.44's own new tests, named explicitly rather
   than silently absorbed or silently dropped.

## Risks / notes
- **Hard join on 4.42, 4.43 and 4.44** — no partial-credit path: 4.42's defect blocks demo step 4, 4.44's
  blocks step 6, and 4.43's makes every file-backed run take minutes per peer.
- **Reuse 4.41's hard-won methodology instead of re-deriving it** (full detail in that task's Status
  section):
  - **OS keystore works in this sandbox** with `apt-get update` first, then `gnome-keyring` + `dbus-x11`,
    then `gnome-keyring-daemon --components=secrets --unlock` (**not** `--start --unlock` together — the
    daemon rejects that combination) under a `dbus-launch`-started session bus with `$HOME` pointed at a
    private, empty scratch directory. 4.38's earlier "impossible here" finding was real but narrower than
    it looked. Re-verify rather than assume, and say which you observed.
  - **The initiator-role trap:** `worker::run_send_message` picks the X3DH initiator by
    `account_pub.as_slice() <= peer_pubkey.as_slice()` — a **raw-byte** comparison.
    `meridian-identity`'s base32 alphabet (`abcdefghijklmnopqrstuvwxyz234567`) makes digits sort *after*
    letters by value but *before* them in ASCII, so comparing the two `mrd1:` id **strings** disagrees
    with the app about half the time. Decode the key part properly and compare raw pubkeys. A driver that
    gets this wrong produces a real-looking but bogus "message not sent: … no prekey preamble" failure.
  - A screen-scraping driver should parse the terminal buffer (4.41 used `pyte`) and match narrow,
    single-line anchors — "mrd1 id:" and "or QR image path:" render on separate lines, and the Chat
    title's em-dash is one character.
  - Both peers must run **concurrently** (offline delivery is T07, not built — a message routed while the
    recipient isn't connected is simply dropped), synchronized once both reach a live `Screen::Main`.
- **Distinguish driver bugs from product defects before reporting anything.** 4.41 found and fixed two of
  its own driver bugs before trusting any result; hold the same bar.
- Nothing from the driver needs to be checked in (4.41's precedent) — but the transcripts quoted in Status
  must be real output, not reconstructed.
- **Definition of Done gate 9:** no new TUI surface — this is a verification task (stated explicitly).

## Tests
`cargo build --workspace` · `bash tools/check-docs.sh` · `cargo test -p meridian-tui --all-targets
--no-fail-fast` · `cargo test -p meridian-cli --bins` · `cargo test -p meridian-tui --test
live_session_e2e --test live_session_file_backed --test republish_bundle` · the new targets 4.42 and 4.44
added · `bash tools/lint-tui-no-cli.sh` · `bash harnesses/mitm-sim/run.sh` (T08 regression) · `cargo fmt
--check` · `cargo clippy --workspace --all-targets -- -D warnings` · a genuine live two-account `meridian
tui` PTY run per the demo script, both account types, plus the restart and `--export-json` steps.

## Reviews
- **reviewer** (combined) — same framing as 4.28/4.38/4.41: a final consistency pass across the whole
  phase's diff, plus an independent re-read of any defect this attempt reports.
- **test-engineer** — owns the independent live-run confirmation and the full regression suite, mirroring
  4.38's and 4.41's split of duties. This is the second of the two independent runs; it must be genuinely
  independent, not a re-reading of the implementer's transcripts.

## Links
- Feature spec: [17-terminal-tui-client.md](../../architecture/features/17-terminal-tui-client.md)
- Design: [tui-client.md §11](../../architecture/tui-client.md#11-current-implementation-status-as-of-task-438--the-phases-second-exit-gate-attempt)
- Prior art: [4.28](./4.28-docs-sync-acceptance-demo.md) (first attempt, non-passing) ·
  [4.38](./4.38-t17-acceptance-demo-closure.md) (second, non-passing) ·
  [4.41](./4.41-t17-acceptance-demo-closure-attempt-3.md) (third, non-passing — methodology to reuse)
- The fixes this attempt re-verifies: [4.42](./4.42-post-accept-chat-affordance.md) ·
  [4.43](./4.43-file-backed-republish-performance.md) · [4.44](./4.44-chat-history-load-on-open.md)
- PR: <filled at /next-task time>

## Status
Pending.
