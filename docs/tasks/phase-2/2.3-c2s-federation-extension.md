> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/06-cross-org-federation.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 2.3 — c2s extension for federation (hint fields, error codes, vectors)

**Phase:** 2 — Cross-Org Federation · **Feature:** T06 — Cross-Org Federation · **Depends on:** 2.2 · **Status:** done

## Goal
Extend the client↔server contract so a client can name a **foreign** target through its **own**
server — the wire encoding of the routing invariant — as one atomic, vector-checked change to a
contract existing clients already speak.

## Scope
- **In:** `Fetch.hint`; `RouteBody.to_hint`; the federated `Deliver` origin field per 2.1's decision;
  new error codes (`fed_denied`, `fed_unreachable`, `not_found_at_hint`); backward compatibility for
  existing hint-less clients; doc + vector regeneration. Also: resolve the delegated
  [rendezvous-protocol-v1 §8](../../api/rendezvous-protocol-v1.md) `TODO: confirm` by **re-deferring
  the normalized-schema + Postgres question to T07**, with its reason recorded.
- **Out:** any server behaviour that consumes the new fields (2.7, 2.8); client error copy (2.9).

## Deliverables
1. `apps/proto/src/msg.rs`: optional `hint` / `to_hint` fields
   (`#[serde(default, skip_serializing_if = ...)]` so existing vectors stay byte-identical when
   absent) plus the new `error_codes` constants.
2. [docs/api/rendezvous-protocol-v1.md](../../api/rendezvous-protocol-v1.md): op table, error-code
   list, §4 routing, §8 simplifications — including the T07 re-deferral and its reason.
3. The matching comment update in `apps/rendezvous/src/store/sqlite.rs`.
4. Reconciliation of [wire-protocol.md §2](../../api/wire-protocol.md), which specifies
   `fetch_bundle {target, hint}` and `deliver {from_server, blob}` while the implementation has
   `Fetch{target}` and `Deliver{from}` — spec and code disagree in exactly the fields federation
   depends on. Make them agree; 2.1 names which shape is canonical.
5. Regenerated `test-vectors/*` — assert **no diff** for the absent-field cases.
6. `apps/proto/tests/roundtrip.rs` cases: hint present / absent / empty / over-length.

## Risks / notes
- The hint crosses the wire as a **plain domain string**. The server must never parse `mrd1:` IDs —
  that needs `meridian-identity` and breaks `tools/lint-server-no-core.sh`. Parsing stays client-side
  in `apps/identity/src/id.rs`. State this in the doc.
- `TODO: confirm`: max hint length, and whether the server validates LDH form. `validate_hint` already
  exists client-side; duplicating it server-side is the dependency trap.
- The new `Fetch` field sits next to the `tamper` test hook — keep the F17 discipline (feature-gated,
  absent from release builds, and covered by the package-scoped default-features CI step).
- **Why T07 and not here:** Feature 06 adds no new persisted state — the federation map, policy and
  allowlist are config, rate limits are in-memory, reachability is live `Registry` state, and the
  bundle server B serves is the blob it already stores. T07's mailbox is the first feature that needs
  per-envelope rows, TTL sweeps and quota accounting, i.e. the first consumer a normalized schema
  would have.

## Tests
```
cargo nextest run -p meridian-proto
cargo run -p xtask -- vectors && git diff --exit-code test-vectors/
cargo nextest run -p meridian-rendezvous
bash tools/lint-server-no-core.sh
bash tools/check-docs.sh
```

## Reviews
- **architect** — required; a versioned change to a **shipped** contract, plus the compatibility story.
- **security-reviewer** — no new client identifiers on the wire.
- **code-reviewer**.

## Links
- Feature spec: [06-cross-org-federation.md](../../architecture/features/06-cross-org-federation.md)
- Contracts: [rendezvous-protocol-v1.md](../../api/rendezvous-protocol-v1.md) ·
  [wire-protocol.md](../../api/wire-protocol.md)
- ADRs: ADR 0017 (from task 2.1)
- PR: [#35](https://github.com/hansajayathilaka/meridian/pull/35)

## Status

**Done.** `apps/proto/src/msg.rs` gains `Fetch.hint: Option<String>` and
`RouteBody.to_hint: Option<String>` (both `#[serde(default, skip_serializing_if =
"Option::is_none")]`), plus three new error codes: `fed_denied`, `fed_unreachable`,
`not_found_at_hint`. `Deliver` is **unchanged** — no new field — per ADR 0017 C2's decision that the
canonical c2s push stays `Deliver{from, blob}`; only its doc comment now records that for a
federated route `from` is the foreign server's relayed assertion (ADR 0017 (b)/C2/C6).

- Both new fields are plain domain strings, never parsed server-side — `mrd1:` ID parsing stays in
  `apps/identity/src/id.rs::validate_hint`, keeping `tools/lint-server-no-core.sh` green. The wire
  type places no length/LDH constraint on either field; enforcement (if any) is server behaviour
  explicitly deferred to whichever task first needs it (2.7).
- Backward compatibility is test-proven, not just claimed: `apps/proto/tests/roundtrip.rs` asserts
  the encoded CBOR map omits the `hint`/`to_hint` key entirely (not a null) when absent, so existing
  hint-less clients are byte-identical on the wire. Three call sites
  (`apps/signaling/src/client.rs`, `apps/cli/src/opacity.rs`) updated to pass `None` explicitly.
- Docs reconciled: `docs/api/rendezvous-protocol-v1.md` (op table, new "Federation hints (T06)"
  subsection under §4, §8's `TODO: confirm normalized schema + Postgres` resolved — **re-deferred to
  T07**, reasoning recorded) and `docs/api/wire-protocol.md` §2 (`fetch_bundle{target,hint?}` /
  `route{to,to_hint?,blob}` / `deliver{from,blob}`, matching ADR 0017 C2's canonical naming — the
  stale `deliver{from_server,...}` duplicate is gone). `apps/rendezvous/src/store/sqlite.rs`'s header
  comment resolved identically. `docs/api/federation-protocol-v1.md` §6 updated to note 2.3 has since
  landed the c2s reconciliation it had deferred.
- Scope honored: zero diff in `apps/rendezvous/src/ws.rs` (grepped — no reference to either new
  field or the new error codes anywhere under `apps/rendezvous/`); no client error-copy taxonomy
  (2.9's job).

### Reviews
- **architect** — ✅ consistent, no revision needed. Verified ADR 0017 C1/C2/C6 on the c2s side,
  confirmed the `Deliver`-unchanged decision is ADR-mandated (not an asymmetry bug), confirmed no
  dependency-graph risk, and independently verified the byte-identity backward-compat claim via the
  new tests.
- **security-reviewer** — ✅ APPROVE, no changes required. Confirmed no new correlation surface or
  contact-graph materialization, the "never parse `mrd1:` server-side" invariant holds, the deferred
  length/LDH validation is a sound posture for a contracts-only task (not a quietly-introduced gap),
  and the `Deliver.from` federated-provenance doc comment accurately restates ADR 0017 without over-
  or under-claiming. Flagged one forward-looking, non-blocking note for 2.7's review: enforce a
  length/form bound server-side once `hint`/`to_hint` are actually read.
- **code-reviewer** — ✅ APPROVE, no blocking or should-fix findings. Independently re-ran every test/
  lint/doc command plus a `--no-default-features` build to confirm the F17 tamper-hook gate is
  unaffected, grepped for every `Fetch{`/`RouteBody{` construction site to confirm none were missed,
  and confirmed `WIRE_VERSION` correctly was not bumped (purely additive, backward-compatible
  optional-field extension per wire-protocol.md §1's unknown-optional-fields policy). Two
  informational nits, both addressed or explicitly non-blocking: the federation-protocol-v1.md §6
  present-tense reference (fixed above) and the test's byte-substring backward-compat check being a
  sound-but-indirect proxy (left as-is; a stricter check would need a `test-vectors/*` fixture this
  task correctly doesn't add, since none exists yet for `Fetch`/`RouteBody`).

### Verified
`cargo test -p meridian-proto` (8/8 pass, incl. 3 new hint tests) · `cargo run -p xtask -- vectors &&
git diff --exit-code test-vectors/` (clean — no fixture covers Fetch/RouteBody today, so this is
correctly a no-op; the roundtrip tests are the real backward-compat proof) · `cargo test -p
meridian-rendezvous` (20 passed, 1 pre-existing ignored capacity test) · `bash
tools/lint-server-no-core.sh` (pass) · `bash tools/check-docs.sh` (clean, no broken links) · `cargo
test --workspace` (full tree green) · `cargo clippy --all-targets -- -D warnings` + `cargo fmt
--check` (clean) · `bash tools/lint-no-serde-on-blob.sh` + `--selftest` (pass).
