# T1.8 — Feature-gate the bundle-tamper hook out of release

> **Phase:** [1 — CI gates](./README.md) · **Plan:** [index](../README.md)
> **Tags:** [SEC] · **Depends on:** — · **Status:** ☐ not started

**Scope.** Remove the key-substitution code path from production binaries.

**Touches.** `apps/rendezvous/src/ws.rs` (L230–235), `apps/rendezvous/src/config.rs` (L28),
`apps/proto/src/msg.rs` (`Fetch.tamper`). **[SEC]** signaling/key trust. security-reviewer.

**Deliverables.** `substitute_bundle` + `tamper` honor path behind a `test-hooks` cargo feature excluded
from `--release`; startup refuses `allow_test_tamper=true` unless `domain=="localhost"`.

**Tests.** `mitm-sim` still passes with the feature on; a release build has no tamper symbol (compile
check).

**Verification (DoD).** 4.

**Pre-build review.** security-reviewer.
