> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/09-file-transfer.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 11.9 — Confirm `soak-file-transfer.yml` ran clean post-10.18 on a real runner

**Phase:** 11 — Review of Phase 10 · **Feature:** review fix (F9) · **Depends on:** — · **Status:** pending

**User surface:** none — docs-only confirmation task (Definition of Done gate 9 doesn't apply).

## Goal
Record a human/devops-observed confirmation that `soak-file-transfer.yml` has run clean on a real runner
since task 10.18's SCTP fix landed, replacing the doc's stale "expected red until the SCTP fix lands"
framing with the actual post-fix signal.

## Scope
- **In:** `docs/testing/soak-file-transfer-throughput.md`'s `TODO: confirm` — a human or devops session
  observes the workflow's real run history (GitHub Actions run history, not reachable from an agent
  session, per the existing branch-protection `TODO: confirm` precedent in `docs/tasks/README.md`'s
  carry-forward section) and records the result.
- **Out:** no code or workflow change — this is a docs-only confirmation task, mirroring the
  branch-protection precedent's exact shape ("check GitHub Settings → ... and update that doc's
  `TODO: confirm` once observed").

## Deliverables
1. `docs/testing/soak-file-transfer-throughput.md` updated once observed: either confirming clean runs
   post-10.18 (with the observed run's date/result), or recording that it is still red/hasn't run, with
   the reason.

## Risks / notes
This task cannot be completed by an agent session alone — no tool available to an agent can read GitHub
Actions run history (matching the exact precedent already on record in `docs/tasks/README.md`'s "Live
carry-forwards" section for branch protection). The task file should stay open/blocked until a human or
devops agent with the right access observes and reports back, at which point the doc update is trivial.

## Tests
None (docs-only) — `bash tools/check-docs.sh` after the doc edit.

## Reviews
**devops**.

## Links
- `docs/testing/soak-file-transfer-throughput.md`, `.github/workflows/soak-file-transfer.yml`
- `docs/tasks/README.md`'s branch-protection `TODO: confirm` precedent
- Review report: [review-report.md](./review-report.md) (finding F9)
