<!-- Fix-task for Phase 1 review findings F4 + F18 (merged: both make ci.yml gates non-decorative). -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report findings F4, F18](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.8 — Real CI gates: deny.toml + cargo-deny + blocking clippy

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F4, F18) · **Depends on:** — · **Status:** pending

## Goal
The license gate and clippy gate actually fail CI when they should — closing the "decorative enforcement"
gap that the entire library-choice rationale (AGPL avoidance) depends on.

## Scope
- **In:**
  - **F4:** add `deny.toml` with the AGPL/license-incompatibility rules ADR 0011/0015 describe; replace
    the `echo "TODO cargo-deny check"` supply-chain job at `.github/workflows/ci.yml:62-69` with a real
    `cargo deny check`.
  - **F18:** remove `|| true` from clippy in both `.github/workflows/ci.yml` and the `Justfile`, making
    `cargo clippy --all-targets -- -D warnings` a blocking gate.
- **Out:** fixing new clippy findings (tree is clean today — flipping is free); the metrics/no-serde
  lints (→ 1.9/1.10); the opacity harness (→ 1.11).

## Deliverables
1. `deny.toml` (license allow/deny list matching ADR 0015's AGPL rationale + advisories/bans as sensible).
2. Edited `.github/workflows/ci.yml` (real cargo-deny job; clippy `|| true` removed).
3. Edited `Justfile` (clippy `|| true` removed).

## Risks / notes
Run `cargo deny check` locally first and pin the exact license set the ADR mandates — a too-loose
`deny.toml` re-creates the decorative gate. Confirm clippy is actually clean before flipping (report says
it is). Coordinate the license list wording with 1.1/1.2 so docs and `deny.toml` agree.

## Tests
`cargo clippy --all-targets -- -D warnings` (must pass) · `cargo deny check` (must pass; verify it fails
on a deliberately-added AGPL dep, then revert).

## Reviews
**devops** (CI wiring), **security-reviewer** (license gate matches the AGPL rationale).

## Links
- Report: [F4, F18](./review-report.md)
- ADRs: 0011, 0015 (license rationale)
- PR: <filled at /next-task time>
