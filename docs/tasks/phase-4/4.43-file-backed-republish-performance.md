> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/17-terminal-tui-client.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 4.43 — File-backed prekey republish performance (188 s → seconds)

**Phase:** 4 — Verification & Trust + Terminal TUI Client · **Feature:** T17 — Terminal TUI Client (gap closure, fix for 4.39's recorded, unfixed defect as measured live by 4.41) · **Depends on:** 4.39, 4.40 · **Status:** done

## Goal
Stop a file-backed account from waiting **~188 seconds on "Unlocking"** at every single session start.
Task 4.39's `worker::republish_bundle` drives 101 real signatures (1 SPK + `DEFAULT_OTK_COUNT` = 100
OTKs) through a raw, per-call-scrypt-unwrapping `FileSecretStore`; give that one call an
already-unwrapped signing store so the republish costs O(1) scrypt work instead of O(101), bringing a
file-backed session start into the same range as onboarding's own publish (~2.1 s, measured by 4.41) and
an OS-keystore account (< 3 s total).

## The design decision — already made, at plan time
4.39 deferred this fix, reasoning it required the "extending in-memory key residency" security decision
reserved for 4.40's consult. **An architect consult during the `/plan-phase` call that created this file
resolved it.** Outcome recorded here, binding on the implementation.

**Verdict: settled by 4.40's consult — no new consult, no new ADR** for the narrow shape below, because
that shape *reduces* residency rather than extending it. One boundary is binding (see "The boundary").

### 4.40's `live_store` cache is the WRONG mechanism — do not reach for it
`apps/store/src/file.rs`'s `FileSecretStore::use_key` and `derive_key` both call `self.decrypt_seed()?` —
a full age/scrypt unwrap — on **every single call**. 4.40's own reviewer already caught this and corrected
that task's `CountingStore` doc comment: the cache saves a second `FileSecretStore` *construction* and a
second passphrase round-trip, **not KDF compute**. Caching a `FileSecretStore` — in `live_store` or
anywhere else — still pays 101 scrypts. (It is also why 4.41 measured file-backed add-contact at 3.6 s and
accept at 5.5 s versus 0.2 s on OS-keystore.)

The correct mechanism already exists in this module: `worker::open_store_for_bulk_signing` →
`export_seed` **once** → `MemorySecretStore`, whose `use_key`/`derive_key` are a `HashMap` lookup plus the
op. O(1) scrypt instead of O(prekeys). This is exactly what makes onboarding's own `PublishBundle` take
2.1 s.

### Type erasure at `InboundHandoff` blocks the naive fix — 4.39's claim verified
`SecretStore` (`apps/store/src/lib.rs`) has exactly four methods: `store`, `use_key`, `nonextractable`,
`derive_key`. `export_seed` is **inherent** on `FileSecretStore` and `OsSecretStore`, deliberately —
`file.rs`'s own doc states the reason: *"Not on `SecretStore`, so enclave-backed stores never expose it."*
`InboundHandoff.store: Box<dyn SecretStore>` is therefore a hard wall: neither `republish_bundle` nor
`run_worker` can unwrap once from what they are handed.

**So the fix must land where the concrete type is still in scope — inside `inbound_handoff`**, whose
`Effect::Unlock` arm still holds `boxed.request.keyfile` / `boxed.request.passphrase` and constructs the
`FileSecretStore` itself. This is also the right place on the passphrase-handling rule: `inbound_handoff`'s
own doc comment already justifies reading that passphrase there ("this *is* that same round trip, just
read a second time"). **Do not re-thread the passphrase further down into `republish_bundle`** — that
would newly violate `UnlockRequest`'s "a live passphrase must never cross more than one effect round
trip" invariant.

### The recommended shape
1. Extract the unwrap-once body of `open_store_for_bulk_signing` into a helper taking an **explicit
   keyfile path** (`inbound_handoff` has `request.keyfile`; the existing function hardcodes
   `default_keyfile_path()`), and have `open_store_for_bulk_signing`'s `File` branch delegate to it —
   one implementation, not two.
2. Add a field to `InboundHandoff`, e.g. `bulk_signing_store: Box<dyn SecretStore>`:
   - `Effect::Unlock` arm (file-backed): the one-time-unwrapped `MemorySecretStore`, keyed by
     `descriptor.label` — matching the existing `handle: KeyHandle::from_label(&descriptor.label)`
     exactly (`MemorySecretStore::store` keys by label), so no handle plumbing changes.
   - `Effect::LoadSession` arm (OS-keystore): a second `OsSecretStore::new(&service)` — free, no branch
     at the call site.
   - Leave `InboundHandoff.store` **unchanged** (still the `FileSecretStore`) so `run_inbound_loop`'s
     behavior is byte-for-byte as shipped.
3. `run_worker` passes `handoff.bulk_signing_store.as_ref()` to `republish_bundle`, then **drops it before
   spawning `run_inbound_loop`**. `MemorySecretStore` holds `Zeroizing<Vec<u8>>`, so the seed is zeroized
   on drop — **raw-seed residency ends at republish completion (~2 s), not process exit.**

**Correctness check, already verified at plan time:** `republish_bundle` uses the store for three things —
the connect-time auth signature, the 101 bundle signatures, and `load_chat`/`save_chat`'s at-rest sealing.
All three derive from the same Ed25519 seed: `FileSecretStore::derive_key` is
`derive_key_from_seed(&self.decrypt_seed()?, info)` and `MemorySecretStore::derive_key` is
`derive_key_from_seed(seed, info)` — identical output for the same seed, so the resealed `sessions.bin`
stays readable by every other handler's `FileSecretStore` path. No migration, no compatibility risk.

### Why this needs no new security sign-off
The question 4.39 deferred was *"may we extend in-memory key residency?"* Scoped as above, this task
**does not extend residency at all** — it reduces it:
- The raw seed is **already materialized 101 times** in this exact call path today (`decrypt_seed()` →
  `Zeroizing<Vec<u8>>` per `use_key`), across 188 seconds. Holding it once for ~2 s is strictly less
  aggregate exposure of identical material.
- The passphrase — the *stronger* secret (passphrase + on-disk keyfile ⇒ seed) — is **already** resident
  for the whole process life in two places: `InboundHandoff`'s `FileSecretStore` (4.35) and
  `OnboardingSession::live_store` (4.40).
- The exact pattern is **already shipped in this module** with review sign-off:
  `open_store_for_bulk_signing` → `PendingConnection::store` (4.30), mirroring `apps/cli/src/main.rs`.
- Threat-model residual **A6** (live-endpoint compromise) already covers this class; 4.40's consult read
  both `threat-model.md` and `anonymity-and-retention.md` in full and found no "must never" contradiction.

### The boundary (binding)
The **narrow** shape above proceeds directly. If the implementer instead **widens** it — putting the
`MemorySecretStore` into `live_store` (to make the eleven contacts/trust/chat handlers scrypt-free) or
handing it to `run_inbound_loop` (to make per-delivery signing scrypt-free) — that **does** change what
4.40's consult approved, from "session-lifetime passphrase" to "session-lifetime raw seed", and needs a
short delta sign-off from **security-reviewer** before landing. Both are genuinely attractive follow-ups
(they would fix the 3.6 s add-contact / 5.5 s accept latencies), but they belong in **their own task**,
not folded into this one. Record the deferral explicitly rather than leaving it invisible.

## Scope
- **In:** the recommended shape above, confined to `apps/tui/src/worker.rs` (`republish_bundle`,
  `inbound_handoff`, `InboundHandoff`, `open_store_for_bulk_signing`) plus `apps/tui/src/lib.rs`'s
  `run_worker` wiring, with the test coverage and measurement in Deliverables 3–5. The `StoreKind::Os`
  branch of every function touched stays byte-for-byte unchanged.
- **In (a second, same-code-path question to decide and document, `TODO: confirm`):** `run_worker`
  currently `await`s `republish_bundle` **before** forwarding the completed outcome to `App` (the
  `replies.send(AppEvent::Worker(..))` call sits after the `inbound_started`-guarded branch), so the whole
  republish latency is on the critical path to `Screen::Main` — which is *why* 4.41 observed a frozen
  "Unlocking" screen rather than a slow background republish. After the store fix it becomes ~2–3 s,
  tolerable. Moving `replies.send(...)` ahead of the republish is mechanically easy
  (`inbound_handoff(&outcome)` already borrows before the move) and would reach `Screen::Main`
  immediately, at the cost of a ~2–3 s window where the worker still can't service a new effect. Decide,
  and document the reasoning as an explicit choice rather than a silent one. Note that `chat_state_lock()`
  already serializes every `sessions.bin` writer, so concurrency is not the hazard here. If the ordering
  changes, say explicitly what it does to the `inbound_started` one-shot guard that 4.39's
  `republish_only_fires_once_per_session_via_the_inbound_started_guard` test pins.
- **Out:** the OS-keystore path (already fast; must stay byte-for-byte unchanged); onboarding's own
  `handle_register`/`handle_publish_bundle` path (already correct, already using
  `open_store_for_bulk_signing`); **the wide shape** described under "The boundary" (its own task, gated
  on a security-reviewer delta sign-off); any change to `DEFAULT_OTK_COUNT`, bundle lifecycle,
  OTK-exhaustion or republish-frequency policy (protocol policy, not a perf fix); unifying the two live
  `FileSecretStore` copies a session now holds (4.40's own explicit `TODO: confirm`, still out of scope);
  4.42's Defect C; 4.44's chat-history gap; any new `Effect`/wire type.

## Deliverables
1. The helper extraction + `InboundHandoff.bulk_signing_store` + `run_worker` wiring, per the recommended
   shape. `InboundHandoff.store` unchanged; OS-keystore branch unchanged.
2. The recorded answer to the inline-vs-off-critical-path question above, in the Status section.
3. **A real, recorded measurement** in the Status section: wall-clock time from the completed
   `Effect::Unlock` to `run_inbound_loop`'s spawn (and to a rendered `Screen::Main`) for a **file-backed**
   account, **before and after**, measured on the same machine in the same session. The "before" number
   must be re-measured, not copied from 4.41 — a claimed speedup with no locally-measured baseline is not
   evidence. Record the machine/conditions so 4.45 can compare like with like.
4. Test coverage extending `apps/tui/tests/republish_bundle.rs`:
   - a **file-backed** republish that actually completes and whose republished secrets are resolvable by
     `ChatState::open_inbound` — the test 4.39 could not write (its
     `inbound_handoff_also_produces_a_working_handoff_for_a_file_backed_account` deliberately never calls
     the real `republish_bundle` against a file-backed store, because it took > 90 s and climbing);
   - a **derivation-count** assertion in the spirit of 4.40's `CountingStore`, proving the expensive
     unwrap happens once, not 101 times. State the claim accurately: per 4.40's own review-outcome
     correction, `FileSecretStore::decrypt_seed` runs on *every* `use_key`/`derive_key` call regardless of
     caching, so the property to assert is "one `export_seed` / one expensive-store construction for the
     whole republish", not a vague "the cache prevents scrypt".
   - an assertion that the `MemorySecretStore` is **dropped before `run_inbound_loop` is spawned** (the
     residency bound this task's security rationale depends on).
5. The existing failure-path guarantee stays proven: 4.39's
   `republish_bundle_fails_cleanly_without_panicking_on_an_unreachable_server` must still pass, and if the
   wiring/ordering changes, add coverage that a failed or slow republish still never prevents
   `run_inbound_loop` from starting.
6. Docs: `worker::republish_bundle`'s own doc comment and 4.39's "Update (task 4.39)" note in
   `docs/architecture/tui-client.md` §11 updated to state this performance defect is closed and how (§10
   and §11's original text stay frozen as historical record, per the existing convention);
   `docs/tasks/phase-4/README.md`'s T17 exit-criteria bullet updated to record it. **The exit-criteria
   checkbox stays unflipped** — that's [4.45](./4.45-t17-acceptance-demo-closure-attempt-4.md)'s job.

## Risks / notes
- **Never log, `Debug`-print, or persist the unwrapped store or the exported seed.** Follow the existing
  redaction precedents (`OnboardingSession`'s hand-rolled `Debug`, `crate::session::LiveSession`'s
  redaction test, `run_worker_account.rs::effect_debug_never_leaks_a_passphrase`). The new
  `InboundHandoff` field needs its own redaction coverage if that type is `Debug`-reachable.
- **Do not weaken `open_account_store`'s fail-closed `None` branch** or any of the three
  `*_fails_closed_with_an_actionable_message` tests.
- **`chat_state_lock()` discipline in `republish_bundle` must survive any re-ordering** — `sessions.bin`
  is one serialized document and `run_inbound_loop` touches it on its own tokio task.
- Only a latency change is user-visible; **there is no new TUI surface** for Definition of Done gate 9 to
  register (stated explicitly, per the gate's own "no user surface? say so" rule). The improvement shows
  up on the existing Unlock screen.
- Land **before** 4.42 if convenient: it has no open design question, and it cuts every subsequent live
  file-backed verification cycle from ~190 s/peer to ~3 s/peer — which 4.42, 4.44 and 4.45 all pay
  repeatedly. No logical dependency either way.

## Tests
`cargo test -p meridian-tui --test republish_bundle` (extended — the narrowest loop) · `cargo test -p
meridian-tui --test live_session_file_backed --test live_session_e2e` · `cargo test -p meridian-tui
--test run_worker_account` · `cargo test -p meridian-tui --all-targets --no-fail-fast` · `cargo test -p
meridian-cli --bins` · `cargo fmt -p meridian-tui -- --check` · `cargo clippy -p meridian-tui
--all-targets -- -D warnings` · `bash tools/check-docs.sh` · plus the timed live run for Deliverable 3
(real `target/debug/meridian tui` against `target/debug/meridian-rendezvous --config
apps/rendezvous/demo.toml`, file-backed account, before and after).

## Reviews
- **reviewer** (combined). **security-reviewer** confirms the shape stayed narrow (residency reduced, not
  extended) and that "The boundary" above was not silently crossed; **architect** covers the `run_worker`
  republish/inbound-loop ordering and the `inbound_started` one-shot contract from 4.35/4.39;
  **code-reviewer** covers correctness of the store plumbing, the drop-before-spawn ordering, and that the
  OS-keystore branch is genuinely untouched.
- **test-engineer** — owns the first-ever completing file-backed republish test and the before/after
  timing evidence, at the same evidentiary bar 4.38/4.41 set for live claims.

## Links
- Feature spec: [17-terminal-tui-client.md](../../architecture/features/17-terminal-tui-client.md)
- The defect: [4.39's own Status section](./4.39-prekey-bundle-republish.md) ("Genuine finding, out of
  this task's fix-scope") and [4.41's Evidence 2](./4.41-t17-acceptance-demo-closure-attempt-3.md)
  (188.0 s / 188.8 s, measured live and repeatedly)
- The cleared design precedent: [4.40](./4.40-file-backed-live-session-store.md) (its consult, its binding
  modifications, and its review-outcome correction about what caching does and does not save)
- Design: [tui-client.md §11](../../architecture/tui-client.md#11-current-implementation-status-as-of-task-438--the-phases-second-exit-gate-attempt)
- Security docs the plan-time decision checked: [threat-model.md](../../security/threat-model.md) ·
  [anonymity-and-retention.md](../../security/anonymity-and-retention.md)
- PR: <filled at /next-task time>

## Status
**Done.** Landed as one commit on `claude/next-task-queue-mownq2`. The narrow shape was implemented as
specified; **"The boundary" was not crossed** and both widenings behind it remain deferred (see below).
Reviews: `reviewer` (combined) returned **PASS** on all three lenses with nothing blocking;
`test-engineer` signed off on Deliverables 4 and 5 after independently falsifying the new tests, and
gated its Deliverable-3 sign-off on this writeup existing.

### Deliverable 2 — the inline vs. off-critical-path decision (recorded, not silent)

**Decision: keep the republish inline.** `replies.send(AppEvent::Worker(..))` stays *after* the awaited
`republish_bundle`, exactly as 4.39 shipped it.

Reasons, in order of weight:
1. **The premise for moving it has evaporated.** The wait it was hiding was ~190 s; it is now ~1.5-2 s,
   squarely inside the "tolerable" band this task's own Scope section names.
2. **Inline preserves a genuinely useful invariant:** by the time `App` renders `Screen::Main`, this
   session's prekey vault is persisted and the inbound loop is live. Sending early would open a window in
   which the app looks ready while a first-contact envelope would still find no vault entry and no
   listener — the exact class of gap 4.38's Defect A was.
3. **It trades a visible wait for an invisible one.** The worker loop is strictly sequential, so during
   that window it services no new effect either way. With `Screen::Main` already rendered the user sees a
   live-looking screen whose first action mysteriously does nothing, instead of an "Unlocking…" screen
   that says what is happening. `tui-client.md` §6 rule 10's spirit ("progress is honest, never
   fabricated") points the same way.
4. **Smaller diff:** this stays a pure store-plumbing perf fix, with no change to the effect/reply
   ordering contract.

**Effect on the `inbound_started` one-shot guard: none, under either ordering.** Stated because it is the
natural worry. The guard is a plain local `bool` in `run_worker`, set *before* the `republish_bundle`
await, inside a strictly sequential `while let Some(effect) = effects.recv().await` loop on one task — no
`Arc`, no shared state, no concurrent reader. No second handoff can be peeked concurrently with the
republish under either ordering; effects dispatched meanwhile queue in the unbounded channel. So 4.39's
`republish_only_fires_once_per_session_via_the_inbound_started_guard` pins an ordering-independent
property and would keep passing verbatim if the send were moved. `chat_state_lock()` discipline is
likewise untouched: `republish_bundle` still holds the lock across its whole `load_chat`/`set_bundle`/
`save_chat` sequence, and `run_inbound_loop` — the only other concurrent `sessions.bin` writer — is still
spawned strictly after that lock is released. (Because the ordering did *not* change, that function's
defensive comment about the inbound loop not yet being spawned also stays accurate rather than going
stale.)

**`TODO: confirm` — the residual this decision does not cover.** The reasoning above holds *when the
rendezvous server answers*. `SignalingClient::connect` (`apps/signaling/src/client.rs`) is a bare
`connect_async(url).await` with **no timeout**, and that connect is `republish_bundle`'s first act. Against
a black-holed or unroutable server (org VPN down, stale `server` in `account.json`) the TUI can still sit
frozen on "Unlocking" for the platform SYN-retry budget (~130 s on Linux defaults) with no progress
indication — the same symptom class 4.41 escalated. **This is not a regression** (4.39's wiring had the
same shape and the same missing timeout) and it does not change the decision, but the wait is not bounded
in the general case and should not be described as if it were. Candidate fixes — wrapping the republish in
`tokio::time::timeout`, or deferring it behind a visible status-bar affordance — belong in **their own
follow-up task**, not here. If a future task does move the republish off the critical path, it should land
with a visible affordance rather than by silently reordering these two statements.

### Deliverable 3 — locally re-measured before/after

**Machine / conditions (recorded so 4.45 can compare like with like):** 4 vCPU Intel Xeon @ 2.10 GHz,
15 GiB RAM, Linux 6.18.5 container. **Debug (unoptimized) builds** of `target/debug/meridian` and
`target/debug/meridian-rendezvous` — the same profile 4.41 used; scrypt is far slower here than in
release, so **only debug-vs-debug comparisons are meaningful**. Real
`target/debug/meridian-rendezvous --config apps/rendezvous/demo.toml` on `ws://127.0.0.1:8443`; load
average 0.5-1.8; file-backed account with a fresh `$MERIDIAN_HOME` per run; `DEFAULT_OTK_COUNT` = 100, so
101 bundle signatures. The "before" runs were executed **before any edit**, on this machine in this
session — **not copied from 4.41**.

**Measurement A — the exact window this deliverable names** (completed `Effect::Unlock` →
`run_inbound_loop`'s spawn), via an instrumented harness replaying `run_worker`'s wiring over real
`worker::dispatch` calls:

| | before | after |
|---|---|---|
| completed `Effect::Unlock` → `run_inbound_loop` spawn | **194.50 s** | **1.92 s** |
| of which `inbound_handoff` (the one `export_seed`) | 0.00 s | 1.86 s |
| of which `republish_bundle` (101 signatures + vault save) | 194.50 s | **0.05 s** |
| onboarding's own `Register`+`PublishBundle` (control) | 1.96 s | 1.98 s |
| `Effect::Unlock` dispatch itself (control) | 1.89 s | 1.89 s |

**Measurement B — the real interactive binary under a PTY** (Python stdlib `pty` + `pyte` screen parsing,
4.41's approach), timed from the frame where `Screen::Unlock(Unlocking)` first renders to the frame where
`Screen::Main` renders — i.e. 4.41's own 188.0 s metric, which includes the ~1.9 s `Unlock` dispatch:

| | before | after |
|---|---|---|
| "Unlocking" rendered → `Screen::Main` rendered | **211.5 s** (1 run) | **3.6 s / 3.8 s** (2 runs) |
| `Screen::Main` → status bar `* connected` | not captured (driver bug, fixed after that run) | 1.9 s / 1.9 s |
| onboarding register+publish (control) | 3.9 s | 3.5 s / 3.6 s |

**Independent confirmation.** `test-engineer` did not re-run the 194.5 s baseline, but ran its own A/B on
the real path — **171.34 s** with the fix reverted vs **12.04 s** with it in place — and its own direct
measurement of this deliverable's window at **1.448 s**, obtained independently and slightly better than
the 1.92 s above. It explicitly recorded the PTY figures (211.5 / 3.6 / 3.8 s) as **taken on report, not
verified**, because the driver's timing summary went to uncaptured stdout.

**Where the residual time actually goes — a correction to the first writeup.** The remaining ~1.5-2 s is
**not** the republish. Measured: `republish_bundle` is **0.05 s**; ~97 % of the window is the single
age/scrypt unwrap `inbound_handoff` now performs. Two consequences worth carrying forward:
- **`MemorySecretStore` residency is ~50 ms, not ~2 s** — so this task's security rationale is *stronger*
  than originally stated, not weaker.
- **A file-backed session start will stay ~1.5-2 s no matter what happens to the republish**, because that
  floor is the one unavoidable unwrap. **4.45 should not attribute any remaining file-backed latency to
  this path.**

**Two caveats on the "before" number, so 4.45 does not over-read it.** (1) The harness's `before` shape
passes `handoff.store` on *post-fix* code: it is a faithful re-creation of 4.39's wiring, not a checkout
of it, and it *includes* the new `inbound_handoff` `export_seed` inside the measured window — inflating
"before" by roughly one scrypt (~1.6 s here). Immaterial against 190 s, but 194.50 s is not a pure 4.39
measurement. (2) The locally re-measured before (194.5 s / 211.5 s) runs somewhat higher than 4.41's
188.0 s, consistent with a slower or more loaded container — which is expected, and is why the baseline
was re-measured rather than reused.

**Harness disposition:** the instrumented harness and the PTY driver are **throwaway and deliberately not
checked in** (kept only in the session scratchpad), following 4.38's and 4.41's precedent. Both need an
externally started server plus env vars and neither asserts anything, so a ~195 s `#[ignore]`d test in
`--all-targets` would be a trap. The *behavioural* half of what they proved is permanently covered by the
one-unwrap test instead, which is deterministic and CI-safe.

### Deliverables 4 and 5 — coverage, and how it was falsified

Four new tests in `apps/tui/tests/republish_bundle.rs`: the first-ever **completing** file-backed
republish, whose republished secrets are re-opened by `ChatState::open_inbound` **through a different
store than wrote them** (the falsifiable form of the plan-time "no migration, no compatibility risk"
check); the **one-unwrap** test, which renames the keyfile off disk after the handoff is built so any
further `decrypt_seed` is physically impossible, with a control probe proving the rename really did
disable that path; the **drop-before-spawn** assertion; and a **failed republish still reaching the
spawn**. 4.39's `republish_bundle_fails_cleanly_without_panicking_on_an_unreachable_server` is byte-
identical and passing.

`test-engineer` broke the code three ways to confirm the tests catch real regressions: deleting the
`drop` line, moving it after the spawn, and reverting the fix entirely — each produced the expected
failure. It also confirmed **no existing assertion was weakened**: the only removed non-comment lines in
the whole diff are the four changed argument lines, the six-line body that moved verbatim into the new
helper, and two `use` lines.

**One real hole the `reviewer` pass found and this task then closed.** The drop-ordering test asserted
`republish < drop < spawn` but never asserted that the *republish call site* passes the bulk store. So
changing `bulk_signing_store.as_ref()` back to `store.as_ref()` would compile, keep every ordering
assertion true, and pass tests that call `republish_bundle` directly — a green suite with the 190 s freeze
restored, the one regression this task exists to prevent. An assertion pinning that call site was added
and falsified against exactly that mutation.

Known, recorded limits of the structural tests: they assert *textual* order, not execution, so a `drop`
that is present, correctly ordered and unreachable would satisfy them; and `include_str!` bakes in on-disk
source at compile time, which couples them to build-time tree state. Both are accepted here because
`run_worker` is private, takes live channels and spawns a forever-loop, and the pattern is established in
this repo (`apps/rendezvous/tests/federation_*.rs`). The runtime half of the claim is carried by the
one-unwrap test.

### Explicitly deferred (recorded, not invisible)

- **The two widenings behind "The boundary"** — putting an unwrap-once store into
  `OnboardingSession::live_store` (would fix 4.41's 3.6 s add-contact / 5.5 s accept) and giving
  `run_inbound_loop` one (per-delivery signing scrypt-free). Both remain unstarted. They convert
  "session-lifetime passphrase" into "session-lifetime raw seed", so each needs **its own task plus a
  security-reviewer delta sign-off**. The implementer records being tempted by the `live_store` one while
  measuring, and not doing it.
- **The unbounded `connect_async`** described under Deliverable 2 above.
- **`InboundHandoff` has no `Debug` impl at all** and one could not compile through
  `Box<dyn SecretStore>`, so the task's "needs its own redaction coverage *if* `Debug`-reachable"
  condition is correctly not triggered. Verified rather than assumed.

**Definition of Done gate 9 (TUI surface): none, as this task's own Risks section states** — the only
user-visible change is latency on the existing Unlock screen. **The T17 exit-criteria checkbox is
deliberately left unflipped; that flip belongs to
[4.45](./4.45-t17-acceptance-demo-closure-attempt-4.md).**
