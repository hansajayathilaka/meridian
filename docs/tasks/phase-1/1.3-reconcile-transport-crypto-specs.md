<!-- Fix-task for Phase 1 review finding F9 + records the F10/F11 wire-level divergence. -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report finding F9](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.3 — Reconcile T03/T04/T05 specs with the composed-ratchet + wire-deferral reality

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F9) · **Depends on:** 1.1 · **Status:** done

## Goal
The T03/T04/T05 feature specs tell the truth: T03 reflects the composed ratchet (not "no hand-rolled
ratchet"), and T04/T05's wire-level P2P/NAT verification is explicitly recorded as deferred pending the
webrtc backend (1.15/1.16) instead of silently diverging.

## Scope
- **In:**
  - `docs/architecture/features/03-e2ee-messaging-relayed.md:12` — drop "audited lib … no hand-rolled
    ratchet," link ADR 0015; record the wasm32/aarch64 validation status honestly (isolated primitive
    probe done; full wasm target build deferred — either wire into CI in 1.6/1.8 or record the deferral).
  - `docs/architecture/features/04-p2p-session-substrate.md` and
    `docs/architecture/features/05-nat-traversal-relay-policy.md` — add an explicit "simulation-only;
    wire-level verification deferred to fix-task 1.15/1.16" note so the divergence is recorded.
- **Out:** implementing the wasm CI job, the webrtc backend, or the NAT matrix.

## Deliverables
1. Edited `docs/architecture/features/03-e2ee-messaging-relayed.md`.
2. Deferral notes in `04-p2p-session-substrate.md` and `05-nat-traversal-relay-policy.md`.

## Risks / notes
Do not silently downgrade acceptance criteria — mark them deferred with a pointer to the closing task,
not deleted. Keep the wasm decision consistent with whatever 1.6/1.8 actually wire.

## Tests
Docs CI link/mermaid check; grep that no spec still claims "no hand-rolled ratchet."

## Reviews
**architect** (spec fidelity + deferral recorded, not hidden).

## Links
- Report: [F9](./review-report.md); related F10/F11
- ADRs: 0015
- Depends: 1.1 (ADR 0015 exists); closed by 1.15 (backend) + 1.16 (observed-candidate enforcement,
  done) + 1.22/1.23 (CLI wiring + wire-level matrix, split from what was originally 1.16)
- PR: opened on branch `claude/next-task-cn1pwx`

## Result
Edited all three feature specs. `03-e2ee-messaging-relayed.md`: dropped the stale "audited lib …
no hand-rolled ratchet, ADR-3" line, replaced with an ADR 0015 citation (composed ratchet from
RustCrypto primitives, vodozemac's API can't be seeded from an external X3DH root key, gated on the
crypto external review); appended a Risks/notes entry recording that the wasm32/aarch64 validation is
currently only an isolated primitive probe — no wasm32 target build exists in
`.github/workflows/ci.yml` yet — deferred honestly to 1.6 or 1.8, not claimed as done.
`04-p2p-session-substrate.md` and `05-nat-traversal-relay-policy.md`: appended "Verification status"
notes (acceptance criteria kept intact, not deleted) recording that current validation is
simulation-only (netns rig + non-webrtc-rs `Transport` test double), with wire-level verification
deferred to 1.15 (webrtc-rs backend) and 1.16 (NAT/relay acceptance matrix) respectively.
`rg -n "no hand-rolled ratchet"` audited — no surviving live claim (only the historical review-report
finding and this task file's own test instruction). `tools/check-docs.sh` passes (1001 links, no
broken links). Reviewed by **architect** — PASS, no false claims introduced, criteria deferred not
deleted, ADR 0015 citation verified accurate against the ADR text, CI claims verified against the
actual `ci.yml` (no wasm32 job present). No code changed.
