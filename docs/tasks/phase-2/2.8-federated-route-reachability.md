> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/06-cross-org-federation.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 2.8 — Federated envelope forwarding + per-request reachability (§3.3 step 5, §3.4)

**Phase:** 2 — Cross-Org Federation · **Feature:** T06 — Cross-Org Federation · **Depends on:** 2.7, 1.32 · **Status:** pending

## Goal
An envelope sealed by Alice reaches Bob at org-b **byte-identically** across the trust boundary, and
org-a can ask org-b "is `K_B` live right now?" **per request** — never a presence subscription.

## Scope
- **In:** `fed_route` on both sides; `fed_reachability` on both sides; delivery into B's local
  `Registry`; the transport-independence invariant; oversized-envelope rejection.
- **Out:** offline queuing — that is T07. A federated `not_connected` stays a **defined error**,
  matching `apps/core/src/signal_relay.rs`'s deliberate hard-error choice.

## Deliverables
1. `federation/inbound.rs`: `handle_fed_route`, `handle_fed_reachability`; `outbound.rs`:
   `route_foreign`, `reachable_foreign`.
2. `apps/rendezvous/src/ws.rs`: `handle_route` branches on `to_hint`.
3. `apps/rendezvous/tests/federation_route.rs`: byte-identical envelope A→B (the
   [wire-protocol.md](../../api/wire-protocol.md) transport-independence invariant, now covering the
   s2s hop); oversized rejected; `closed` origin rejected; reachability is boolean, per-request, **not
   persisted and not logged**.

## Risks / notes
- **This is where 2.1's `from`-attestation decision is cashed.** Whatever ADR 0017 chose, the client's
  `SenderMismatch` check in `apps/core/src/chat.rs` must remain the load-bearing defence, and a forged
  origin from a hostile foreign server must fail closed. **Do not start before 1.32 lands** — its
  harness is what proves this rather than asserting it.
- **Dependency trap.** The server must not decode the envelope to determine `from`; that needs
  `meridian-envelope`. Both `tools/lint-no-serde-on-blob.sh` and its `--selftest` must pass.
- `TODO: confirm`: the maximum envelope size for the oversized-rejection criterion. No figure exists
  anywhere in the docs.
- `TODO: confirm`: replay/de-dup window on the s2s hop, and **who owns it**. Envelope v1 has no `eid`,
  and [ADR 0016](../../adr/0016-envelope-deniability.md) C7 already assigns the `eid` replay-dedup key
  to envelope **v2**. If Feature 06 invents its own, v2 lands a second competing one. Record a single
  owner rather than building both.
- `TODO: confirm`: whether `fed_reachability` is answerable to the *client* or server-internal only.
  A general client-visible probe is a cross-org liveness channel arguably beyond the
  [anonymity ceiling](../../security/anonymity-and-retention.md), and `not_found` vs
  `connected:false` is an existence oracle. Architect call.
- §7.1 and `seq-cross-org-setup.mermaid` both say "deliver (or mailbox if offline)". The mailbox is
  ADR 0007 / Feature 07 — cross-org-offline must resolve to a **defined error** here, not absorb the
  mailbox.

## Tests
```
cargo nextest run -p meridian-rendezvous --test federation_route
bash tools/lint-no-serde-on-blob.sh && bash tools/lint-no-serde-on-blob.sh --selftest
bash tools/lint-server-no-core.sh
```

## Reviews
- **security-reviewer** — required.
- **architect** — §3.4 per-request-only presence and the ADR 0007 boundary.
- **test-engineer** — byte-identity must be asserted on real bytes, not a re-encode.
- **code-reviewer**.

## Links
- Feature spec: [06-cross-org-federation.md](../../architecture/features/06-cross-org-federation.md)
- ADRs: [0016](../../adr/0016-envelope-deniability.md) (C7, `eid` ownership) ·
  [0007](../../adr/0007-offline-mailbox.md) (the boundary this must not cross) · ADR 0017 (task 2.1)
- Depends on: [1.32](../phase-1/1.32-relay-attacks-past-signature.md)
- PR: _filled at /next-task time_
