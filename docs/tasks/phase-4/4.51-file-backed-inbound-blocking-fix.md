> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/17-terminal-tui-client.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 4.51 — Root-cause and fix the file-backed responder's blocking-scrypt hazard (delivery non-determinism + the known interactive-latency finding)

**Phase:** 4 — Verification & Trust + Terminal TUI Client · **Feature:** T17 — Terminal TUI Client (gap closure, fix for 4.50's sixth defect) · **Depends on:** 4.50 · **Status:** in progress

## Goal
Close the sixth exit-gate defect: first-contact message delivery to a file-backed responder from an
OS-keystore initiator is non-deterministically silent or very slow (70 s–260 s, reproduced ~15/15 trials
by 4.50's independent run). Do this by actually finishing the investigation 4.50's reviewer left open
(root cause corroborated as *plausible*, not *closed*) before choosing a fix shape, then implement
whatever that investigation points to — and, if it generalizes cleanly, close the same-class,
already-known `run_mark_verified`/`run_set_petname` latency finding (4.43's own recorded, deliberately
deferred follow-up) in the same pass rather than as a second patch.

## Pre-code consult assessment — recorded here, gated on this task's own investigation, not skipped
**This is a two-phase task with a conditional consult gate, decided in-task before Phase 2's code
lands — mirroring 4.35's and 4.9's own precedent for an in-task (not plan-time) consult**, because this
planning pass genuinely cannot know which fix shape the investigation will produce, and the instruction
that created this task is explicit: let the investigation decide, do not presume the fix shape at plan
time.

**Phase 1 (investigation — Deliverables 1–4) needs no consult.** It reads, measures, and instruments;
it changes no code and touches no key material's residency.

**Phase 2 (the fix) is gated by a binding decision rule, to be applied and recorded in this task's own
Status section before any Phase-2 code lands:**
- **If the investigation concludes the fix is `spawn_blocking`-only** — wrapping the existing
  synchronous `decrypt_seed()`-calling operations (`process_inbound_delivery`'s `use_key`/`derive_key`
  calls, and — see Scope point 2 below — `SignalingClient::handshake`'s `sign()` call as invoked from
  `run_inbound_loop`) in `tokio::task::spawn_blocking`, with **no new caching, no new residency, the
  same seed decrypted the same number of times at the same call sites, freed at the same points as
  today** — **no consult is required.** This is a pure execution-context change, not a security-posture
  change: it does not touch anything either 4.40's or 4.43's consults reasoned about. Mirrors 4.43's own
  reasoning for why its narrow shape needed no new sign-off ("reduces, does not extend, residency" — a
  `spawn_blocking` wrap reduces *nothing* about residency, but it also doesn't extend anything, which is
  the same "no new exposure class" test 4.43 applied).
- **If the investigation concludes the fix requires caching an already-unwrapped seed** (a
  `MemorySecretStore`-shaped object) for `InboundHandoff::store`'s lifetime (making per-delivery signing
  in `run_inbound_loop` scrypt-free) and/or widening `OnboardingSession::live_store` the same way for the
  interactive contacts/trust/chat handlers — **a mandatory security-reviewer delta sign-off is required
  before implementation.** This is not a new judgment call: 4.43's own "The boundary" section already
  named *both* of these exact widenings by description ("putting the `MemorySecretStore` into
  `live_store`" / "handing it to `run_inbound_loop`") and reserved them for "their own task… gated on a
  security-reviewer delta sign-off" — this task is that task, for both. Given the inbound-loop widening
  specifically would extend raw-seed residency to the **full process lifetime** (unlike 4.43's own
  bulk-signing cache, which is dropped after ~50 ms), a materially larger residency window than either
  4.40's or 4.43's consults ever evaluated, **architect input is also required alongside
  security-reviewer** — treat this as a full consult mirroring 4.40's dual-lens shape, not a lighter
  "delta" review alone. **Do not implement first and consult after** (4.40's own binding rule, restated).
- **If the investigation points to a third shape neither bullet above anticipates** (e.g., a runtime
  scheduling change with no residency question at all) — the implementer must record, explicitly and in
  writing in this task's Status section, why that shape does or does not trip either bullet above, before
  proceeding. Silence is not permitted.

## Scope
- **In (Phase 1 — investigation):**
  1. **Remeasure `FileSecretStore::decrypt_seed()`'s real wall-clock cost in this sandbox**, at the
     shipped age/scrypt parameters, with machine/conditions recorded exactly as 4.43's Deliverable 3 did
     ("Machine / conditions" subsection: CPU, RAM, kernel, debug-vs-release build, load average) — this
     task must not reuse 4.43's own ~1.4–1.6 s/unwrap figure without re-measuring it here, since 4.50's
     own carry-forward explicitly flags that figure "may not hold here."
  2. **A complete accounting of every synchronous `decrypt_seed()`-calling call site reachable during a
     live two-peer first-contact exchange for a file-backed responder** — not just the two
     (`process_inbound_delivery`'s `use_key` for `x3dh::respond`, and `derive_key` for `save_chat`'s
     reseal) the 4.50 reviewer traced. **A third call site exists and was not in that accounting**:
     `run_inbound_loop` passes `store.as_ref()` — `InboundHandoff::store`, the raw
     per-call-scrypt-unwrapping `FileSecretStore`, deliberately *not* the unwrap-once
     `bulk_signing_store` (see `InboundHandoff`'s own doc comment) — into
     `SignalingClient::connect(&server, store.as_ref(), &handle, ...)` on **every** initial connect *and
     every reconnect attempt* in the loop (`apps/tui/src/worker.rs` `run_inbound_loop`); `connect`
     immediately calls `handshake`, which calls `sign(store, handle, &to_sign)`
     (`apps/signaling/src/client.rs`) — a third, independent, synchronous `decrypt_seed()` call, paid once
     per (re)connect, not once per delivered envelope. Record this as a table: call site · when it fires ·
     measured cost (from Deliverable 1's remeasurement) · whether it fires once per session, once per
     envelope, or once per (re)connect attempt.
  3. **Rule the reconnect-storm hypothesis in or out, with live evidence, not inference.** Instrument
     (temporarily — no need to ship new production logging unless useful anyway) the responder side to
     record every `AppEvent::ConnectionStatus(ConnectionState::Reconnecting { attempt, max })` event
     during multiple fresh two-peer live repros of the stall (reuse 4.50 Run 2's methodology: OS-keystore
     initiator, file-backed responder, first-contact message, ≥5 fresh trials). If reconnects are
     observed during a stall, correlate their count/timing against the stall window and check whether
     Deliverable 2's per-(re)connect cost (handshake's `sign()` unwrap) arithmetically explains some or
     all of the gap. If **zero** reconnects are observed across repeated repro attempts that still show
     the stall, state that explicitly and do not force the reconnect-storm explanation — redirect toward
     whatever the evidence actually points to (e.g., a stalled TCP read/write below the application layer,
     independent of `ConnectionState`). Confirmed already, do not re-derive: no ping/pong keepalive exists
     anywhere in this codebase (`apps/signaling/src/client.rs`, `apps/rendezvous/src/ws.rs`/`lib.rs`), so
     a reconnect can only fire on a genuine connection-level failure, not an idle-timeout the app itself
     enforces.
  4. **Produce a reconciled wall-clock accounting for the observed 70 s–260 s range**, stating as
     precisely as the evidence allows what fraction is explained by Deliverables 1–3's findings, and
     **naming any residual gap honestly** rather than declaring the investigation closed if it isn't —
     mirroring 4.50's own "root cause not fully closed" honesty and 4.43's "Two caveats" precedent for not
     over-reading a measurement. If in-sandbox packet-capture/strace-level tracing below the app layer
     isn't available or isn't permitted in this environment, say so explicitly and record what could and
     couldn't be checked, rather than guessing.
- **In (Phase 2 — the fix, once Phase 1 concludes and the consult gate above is resolved):** implement
  whatever shape the investigation points to, at minimum closing the delivery-reliability defect for the
  OS-keystore-initiator → file-backed-responder direction. **Opportunistically, in the same diff, close
  the already-known `run_mark_verified`/`run_set_petname` latency finding** (4.43's own recorded,
  deliberately-deferred `live_store`-widening follow-up; independently re-confirmed twice by 4.50's two
  runs) **if and only if** the chosen fix mechanism naturally covers it without a disproportionate scope
  widening. If it does not cleanly generalize, **split it into an explicitly-named follow-up task**
  rather than forcing an ill-fitting shape or silently dropping it — mirrors 4.40's own risk-note
  discipline ("if the refactor doesn't cleanly generalize... split rather than force a one-size-fits-all
  API — note the split explicitly rather than silently narrowing scope").
- **Out:** any change to X3DH/ratchet protocol behavior, wire format, or `ChatError` semantics (this is a
  concurrency/residency fix, not a protocol fix); the reverse direction (file-backed sender → OS-keystore
  responder — already reliable, 9/9 in 4.50's own trials, untouched); a logout/lock feature; any new
  `SecretStore` implementation beyond what the chosen fix shape strictly requires; the
  contacts.json-trust-field-staleness secondary finding 4.50 Run 2 also recorded (unrelated root cause —
  see `docs/tasks/phase-4/README.md`'s "Findings with no task yet"); the `^N`/`^V` stale-keybinding doc
  nit (already tracked there, still unowned, unrelated).

## Deliverables
1. The remeasured `decrypt_seed()` wall-clock cost, with machine/conditions recorded (Scope point 1).
2. The full call-site accounting table, including the newly-identified `SignalingClient::handshake`
   third call site (Scope point 2).
3. The reconnect-storm ruled-in/ruled-out determination, with live-instrumented evidence (Scope point 3).
4. The reconciled 70 s–260 s wall-clock accounting, with any residual gap named honestly (Scope point 4).
5. The recorded fix-shape decision, applying the binding rule in "Pre-code consult assessment" above, and
   — once any required consult clears — the fix itself.
6. Test coverage proving the fix is genuine, not decorative:
   - A **falsifiable concurrency assertion** matching whichever shape lands: if `spawn_blocking`, a test
     proving `run_inbound_loop`'s task can still service a second, concurrently-dispatched event while a
     blocking unwrap is in flight (not just "the operation eventually completed"); if a cache, a
     construction-count test in 4.40's `CountingStore` idiom (one construction, not per-call) plus a
     drop/zeroize-timing assertion bounding the new residency window explicitly, mirroring 4.43's
     Deliverable 4's "dropped before spawn" assertion adapted to this cache's actual bound.
   - A **live, two-peer PTY regression** targeting exactly the defect this task exists to fix:
     OS-keystore initiator → file-backed responder, first-contact message, **≥10 fresh trials**, each
     timed and reported individually (not averaged away) — the same evidentiary bar 4.50 Run 2 used to
     find the defect (~15 trials), now used to prove it's gone. State a concrete pass bound (e.g., every
     trial completes within single-digit seconds) rather than a vague "faster."
7. If Scope's "In (Phase 2)" opportunistic close of the `run_mark_verified`/`run_set_petname` finding
   lands: a locally re-measured before/after for both, in 4.43's Deliverable 3 format, directly comparable
   to 4.50's own recorded figures (~3.4 s / ~6.9 s). If it's split off instead: a named follow-up task
   reference recorded here and added to `docs/tasks/phase-4/README.md`'s "Findings with no task yet" (or a
   new task file, implementer's call, documented either way).
8. Docs: `docs/architecture/tui-client.md` §11's "Update (task 4.51)" paragraph, appended after the
   existing "Update (task 4.49)" paragraph (§10/§11's original text stays frozen, per the established
   convention); `InboundHandoff`'s (and, if touched, `OnboardingSession::live_store`'s) own doc comments
   updated to describe the new residency/execution-context bound, following the existing precedent both
   types' doc comments already set; `docs/tasks/phase-4/README.md`'s T17 exit-criteria bullet updated to
   record the sixth defect closed. **The exit-criteria checkbox itself stays unflipped** — that's
   [4.52](./4.52-t17-acceptance-demo-closure-attempt-7.md)'s job, exactly as every prior wave left it for
   its own exit-gate task.

## Risks / notes
- **`tokio::task::block_in_place` is not a legal alternative to `spawn_blocking` here.** It panics when
  called from a `current_thread` runtime — and `apps/cli/src/main.rs::runtime()` builds exactly that
  flavor (`tokio::runtime::Builder::new_current_thread()`, confirmed). Do not reach for it.
- **`spawn_blocking`'s closure must be `'static + Send`.** Every call site this task's investigation
  names (`process_inbound_delivery`, `SignalingClient::handshake`) currently receives `&dyn SecretStore`
  by borrow, not by owned/`Arc` value — wrapping requires either an owned/`Arc`-cloneable store or
  restructuring the call boundary. This is a real mechanical question the fix phase must resolve, not
  hand-wave; if it forces a wider-than-expected signature change, name that explicitly rather than
  quietly narrowing which call sites get fixed.
- **Do not conflate this task's fix with 4.40's `live_store` or 4.43's `bulk_signing_store`.** If a cache
  is chosen (consult-gated, see above), it must be a *new*, explicitly-scoped and explicitly-bounded
  structure — not a silent widening of either existing cache's documented contract, both of which their
  own doc comments describe precisely and both of which other code currently depends on staying as
  documented.
- **Never log, `Debug`-print, or persist any newly introduced store/cache/seed material.** Follow the
  existing redaction precedents (`OnboardingSession`'s hand-rolled `Debug`, `InboundHandoff`'s "no `Debug`
  impl at all, by construction" pattern). A new type needs its own redaction coverage if it's
  `Debug`-reachable.
- **Describe the mechanism accurately.** `run_inbound_loop`'s `while let Ok(deliver) = client.next_deliver().await`
  is a blocking `await`, not a busy-poll loop — the 4.50 reviewer already corrected a "polling"
  mischaracterization once; don't reintroduce it.
- **`chat_state_lock()` discipline must not be affected** by any `spawn_blocking`/caching change inside
  `process_inbound_delivery` — if that function's structure changes, confirm explicitly that the lock is
  still held across the whole load-open-save sequence, exactly as its own doc comment requires.
- **If in-sandbox packet-capture/strace tracing isn't available**, say so and record what could and
  couldn't be checked (Scope point 4) — do not guess at a below-the-app-layer mechanism and present it as
  confirmed.
- **Definition of Done gate 9:** no new TUI surface expected — this is a reliability/perf fix, state
  explicitly, mirroring 4.40's/4.43's own "no user surface" notes — unless the investigation surfaces a
  case where a visible "connecting/unlocking" status affordance is a genuinely necessary part of the fix
  (not required by default); if one is added, it must register through the TUI extension registry, never
  by editing the TUI core.

## Tests
The narrowest test for whichever fix shape lands (in-module `#[cfg(test)]`, mirroring 4.40's
`CountingStore` idiom, or a concurrency-proving integration test) · `cargo test -p meridian-tui
--all-targets --no-fail-fast` · `cargo test -p meridian-cli --bins` · `cargo test -p meridian-tui --test
live_session_file_backed --test live_session_e2e --test run_worker_trust --test accept_to_chat --test
republish_bundle` · `bash harnesses/mitm-sim/run.sh` (T08 regression) · `bash tools/lint-tui-no-cli.sh` ·
`bash tools/check-docs.sh` · `cargo fmt --check` · `cargo clippy --workspace --all-targets -- -D
warnings` · a genuine, repeated (≥10 trials, individually reported) live two-peer PTY run: OS-keystore
initiator → file-backed responder, first-contact delivery · if the shared latency fix lands: a locally
re-measured before/after for `run_mark_verified`/`run_set_petname`, in 4.43's Deliverable-3 format.

## Reviews
- **reviewer** (combined). **architect** and **security-reviewer** are load-bearing lenses *only if*
  Phase 2's fix shape trips the consult gate in "Pre-code consult assessment" above — their sign-off must
  be recorded in this task's own Status section *before* Phase 2 code lands, not folded into the ordinary
  post-diff pass, mirroring 4.40's own binding sequencing rule. **code-reviewer** always reviews, for
  correctness of whichever call-site plumbing lands and to confirm 4.43's "boundary" was either not
  crossed or was crossed only with the required sign-off.
- **test-engineer** — owns the independent live two-peer repro proving the fix closes the defect. Must be
  genuinely independent (reusing 4.50 Run 2's driver-construction approach as prior art is fine; re-reading
  this task's own transcripts is not), mirroring every prior exit-gate wave's two-independent-runs
  discipline, and owns independent confirmation of the reconnect-storm instrumentation's findings.

## Links
- Feature spec: [17-terminal-tui-client.md](../../architecture/features/17-terminal-tui-client.md)
- Design: [tui-client.md §11](../../architecture/tui-client.md#11-current-implementation-status-as-of-task-438--the-phases-second-exit-gate-attempt)
- The defect this task fixes: [4.50's own Status section](./4.50-t17-acceptance-demo-closure-attempt-6.md)
  (Run 2's own trace, and the reviewer's independent corroboration-but-not-closure pass)
- The already-known, same-class latency finding: [4.43's own "Explicitly deferred" section](./4.43-file-backed-republish-performance.md#explicitly-deferred-recorded-not-invisible)
  (both widenings behind "The boundary," named and reserved for exactly this task)
- The residency precedent this task must not silently widen: [4.40](./4.40-file-backed-live-session-store.md)
  (its consult, its binding modifications, `live_store`'s documented contract)
- Security docs to read if the consult gate fires: [threat-model.md](../../security/threat-model.md) ·
  [anonymity-and-retention.md](../../security/anonymity-and-retention.md)
- ADRs: none anticipated; if the triggered consult concludes otherwise, route through `/adr` before
  continuing, per 4.40's own binding rule
- PR: <filled at /next-task time>

## Status
Pending — scoped by `/plan-phase`'s sixth gap-closure-wave planning pass, against 4.50's own findings.
Not yet implemented.
