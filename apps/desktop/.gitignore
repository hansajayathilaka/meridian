# Generated by tauri-build (permission/capability JSON schemas) at compile time — not source.
/gen/
# Task 12.15 — `ui/`'s `vite build` output, embedded by `tauri.conf.json`'s `build.frontendDist`
# (rebuilt by `beforeBuildCommand`/`beforeDevCommand` on every `cargo tauri build`/`dev`; hashed
# asset filenames change every build). Mirrors `apps/web/.gitignore`'s identical `dist/` exclusion
# for the sibling browser client's own `vite build` output — never source, never committed.
# NOTE: `dist/index.html` is currently still git-tracked from task 12.3's placeholder file (before
# this task added a real build); untrack it (`git rm --cached apps/desktop/dist/index.html`) when
# committing this change, matching `apps/web/dist` (never tracked at all).
/dist/
