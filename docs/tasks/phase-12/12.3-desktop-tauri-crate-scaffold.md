<!-- Copy this file to docs/tasks/phase-N/P.N-<slug>.md and fill every section.
     Shape mirrors the feature specs in docs/architecture/features/. -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [feature spec](../../architecture/features/11-browser-desktop-clients.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 12.3 — `apps/desktop` Tauri crate scaffold (Rust side)

**Phase:** 12 — Browser & Desktop Clients · **Feature:** T11 · **Depends on:** — · **Status:** done

**User surface:** desktop app shell foundation (Rust backend only — no window UI yet, that's 12.15). No
TUI surface applies; this is a new client platform, not a new protocol feature (see
[phase-12/README.md](./README.md#architect-consult-substratestoresigning-decisions-settled-before-task-breakdown)
point 5).

## Goal
Add a new Cargo workspace member, `apps/desktop` (Tauri v2, per [ADR 0010](../../adr/0010-desktop-shell-tauri.md)),
running `meridian-core` in-process and wiring **the existing** `apps/store::OsSecretStore` and
`apps/transport::WebRtcTransport` — per the architect consult's point 1, desktop needs zero new
`SecretStore`/`Transport` code, only Tauri integration. Exposes the `core-api-contracts.md` operation
surface to the WebView via Tauri commands/events.

## Scope
- **In:** `apps/desktop` crate scaffold; `Cargo.toml` root `members` update; construction of
  `OsSecretStore::new(...)` and `WebRtcTransport::new()` exactly as `apps/cli` already does (same
  crates, same feature flags — build with `--features webrtc` on `meridian-transport`/`meridian-core`,
  matching the CLI); Tauri `#[command]` handlers covering account lifecycle, chat send/receive,
  contacts/trust, and stream/file operations from `core-api-contracts.md`; Tauri events for inbound
  message/state-change push to the frontend.
- **Out:** any new `SecretStore`/`Transport` implementation (there is none — reuse only); the TS-side
  adapter consuming these commands (12.6); the actual window UI (12.15); updater/signing config (12.16).

## Deliverables
1. `apps/desktop/Cargo.toml` (new workspace member).
2. `apps/desktop/src/main.rs` — Tauri app entry, `OsSecretStore`/`WebRtcTransport` construction.
3. `apps/desktop/tauri.conf.json` — minimal config (window/bundle identifiers; updater config deferred
   to 12.16).
4. `apps/desktop/src/commands.rs` — Tauri `#[command]` handlers.
5. Tests exercising the command handlers directly (not through a real WebView) against a real
   `meridian-core` session over `LoopbackTransport` for determinism, confirming the command layer
   correctly marshals requests/results without altering `meridian-core` behavior.

## Risks / notes
Secret material (raw seeds, session keys) must never cross the Tauri IPC boundary in plaintext — only
operation *results* (already the shape `core-api-contracts.md`'s functions return) should ever reach
the JS side. This is the same discipline `apps/cli` already follows (the CLI process never prints key
material outside explicit, logged `export`/`--export-json`-style paths); do not introduce a new leak
surface via a Tauri command that returns raw key bytes.

`TODO: confirm` — the exact Tauri-command naming/event-shape convention isn't specified in any doc;
this task establishes it, and later tasks (12.6, 12.15, 12.16) follow it rather than each inventing
their own.

## Tests
`cargo test -p meridian-desktop`.

## Reviews
**reviewer** (combined pass: architect lens — confirms this is reuse-only per the consult's point 1,
zero new `Transport`/`SecretStore` code landed; security-reviewer lens — mandatory, confirms no secret
material crosses the Tauri IPC boundary in plaintext).

## Outcome
Added `apps/desktop` (`meridian-desktop`) as a new workspace member: a generic `DesktopState<T: Transport>`
command layer (`src/commands.rs`) plus thin, concrete `#[tauri::command]` wrappers (`src/tauri_commands.rs`)
covering account/contact/session/chat/file operations. Constructs `OsSecretStore::new(OS_KEYSTORE_SERVICE)`
and (feature-gated `webrtc`, matching the CLI) `WebRtcTransport::new()` exactly as `apps/cli` already does —
same `OS_KEYSTORE_SERVICE` constant, so accounts are shared between CLI and desktop by design; zero new
`Transport`/`SecretStore` implementations. Established the command/event naming convention later tasks
(12.6, 12.15, 12.16) will follow: `snake_case`, domain-prefixed commands (`account_*`, `contact_*`,
`session_*`, `chat_*`, `file_*`); `domain:event`-shaped, colon-namespaced events. Updater config
deliberately not added (`tauri.conf.json`'s `bundle.active: false`, no updater plugin) — deferred to 12.16.

The combined `reviewer` pass (architect + security-reviewer) returned **PASS** on both lenses, zero
blocking findings, after independently re-running every check (6/6 `meridian-desktop` tests, fmt, clippy
in both default and `--features webrtc` configs, a full `cargo check --workspace`) and directly grepping
every `#[tauri::command]` return type and emitted event payload for `Zeroizing`/`SecretStore`/`KeyHandle`/
raw key bytes — confirmed the per-transfer `Zeroizing<[u8;32]>` key (`k_f`) only ever lives in an internal
`DesktopState` field, never in a `Serialize`d IPC type, corroborated by the implementation's own direct
test assertion that no emitted event ever contains `"k_f"`. Confirmed `meridian-rendezvous`'s dependency
tree is unaffected (still no `meridian-core`, server-no-core invariant intact) and that the `Cargo.lock`
diff's apparent package-removal noise is just alphabetical reordering, not actual removals. The
`FILE_OPEN_HELLO` synthetic first-contact-gate chat message (mirroring `apps/cli/send.rs`'s `HELLO`
precedent exactly) was reviewed as not spoofable and not a new leak surface — a UX-shape `TODO: confirm`
correctly left for task 12.15 (should the file-open handshake get a dedicated non-content wire signal
instead of overloading `ChatContent::Text`?), not a security gap needing to block this task.

## Links
- Feature spec: [../../architecture/features/11-browser-desktop-clients.md](../../architecture/features/11-browser-desktop-clients.md)
- API contract: [../../api/core-api-contracts.md](../../api/core-api-contracts.md)
- ADRs: [0010](../../adr/0010-desktop-shell-tauri.md)
- PR: opened at end of this `/next-task all` batch run
