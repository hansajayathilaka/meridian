# T3.1 — webrtc-rs backend skeleton behind the `Transport` trait

> **Phase:** [3 — Transport backend](./README.md) · **Plan:** [index](../README.md)
> **Tags:** [ADR] · **Depends on:** Phase 1, T0.5 · **Status:** ☐ not started

**Scope.** A real `WebRtcTransport` implementing the trait, feature-gated `webrtc`, data-channel connect
only (no policy/fingerprint yet).

**Touches.** `apps/transport/Cargo.toml` (real dep), `apps/transport/src/webrtc.rs` (new). **[ADR]**
0014/0006. architect approves the dependency + seam before build; devops on the dep tree.

**Deliverables.** Feature-gated backend; two in-process peers connect over a real data channel.

**Tests.** Gated integration test: connect + echo over `webrtc`; default build unchanged.

**Verification (DoD).** 1, 5 (matches ADR 0014 trait seam).

**Pre-build review.** architect.
