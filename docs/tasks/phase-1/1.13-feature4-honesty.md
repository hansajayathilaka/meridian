<!-- Fix-task for Phase 1 review finding F10 (honesty portion). -->
> **Nav:** [tracker](../README.md) · [phase](./README.md) · [report finding F10](./review-report.md) · [Definition of Done](../../../CONTRIBUTING.md)

# 1.13 — Feature 4 honesty: real transport label + truthful SDP test

**Phase:** 1 — Review of Phase 0 · **Feature:** review fix (F10 honesty) · **Depends on:** — · **Status:** done

## Goal
Feature 4's user-visible output and test names stop overclaiming — without waiting on the weeks-long
webrtc backend (1.15). Corrects the blocking overclaims immediately so the honest state is recorded.

## Scope
- **In:**
  - Replace the hardcoded `transport: "webrtc-datachannel"` at `apps/core/src/session.rs:291` with the
    transport actually in use (loopback today), so session info doesn't misreport the substrate.
  - Rename/strengthen `malicious_relay_cannot_touch_inner_sdp` (`apps/core/tests/p2p_session.rs:248-264`)
    so its name matches what it proves — its body currently "just confirms a healthy connect," not an
    active relay-rewrite attack. Rename to reflect reality (e.g. `relay_path_connects_healthily`) with a
    `TODO` pointing at 1.23 (originally 1.16, split — see that task's Status note) for the real
    rewrite-attack test once the backend lands.
- **Out:** implementing the webrtc backend (→ 1.15); the real active-relay-rewrite test (→ 1.23).

## Deliverables
1. Edited `apps/core/src/session.rs` — transport label reflects the live transport.
2. Renamed/annotated test in `apps/core/tests/p2p_session.rs` matching what it actually proves.

## Risks / notes
Land **before** 1.15 to avoid a label conflict when the real backend sets its own transport string. Do
not delete the SDP test — rename + annotate so the coverage intent survives for 1.16.

## Tests
`cargo nextest run -p meridian-core`

## Reviews
**security-reviewer** — first pass NEEDS CHANGES: the same "malicious relay cannot touch the inner
SDP" claim survived, unhedged, as settled fact in three sibling doc comments not touched by the
rename (`apps/core/src/session.rs` module doc, `apps/core/tests/p2p_session.rs` file header bullet,
`harnesses/mitm-sim/run.sh` header). Fixed: reworded all three to state the true, structurally-
guaranteed property (opaque ciphertext, independent of transport backend) while explicitly noting an
automated active relay-rewrite test is still open, tracked at 1.23 (originally 1.16). Re-reviewed clean.
**code-reviewer** — APPROVE, no blocking findings (two non-blocking nits: redundant comment lines in
the renamed test, a stale "datachannel" word in a CLI test comment — the latter fixed inline).

Also added a `Transport::name()` method (no default; every backend states its own name explicitly) as
the mechanism for the honest label, implemented on `LoopbackTransport` → `"loopback"`.

## Links
- Report: [F10](./review-report.md) (honesty portion); backend → 1.15; rewrite test → 1.23 (originally 1.16, split)
- PR: <filled at /next-task time>
